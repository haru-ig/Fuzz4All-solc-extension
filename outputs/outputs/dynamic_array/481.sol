pragma solidity ^0.8.0;
contract test10
{
    address x;
    bytes[] public MyArray;

    constructor() public
    {
        myOtherArray = new bytes[](3);
    }

    function addMyArray(bytes memory _newMyArray)
    {
        myArray = new bytes[](1);
    }
    function addMyArray()
    {
        myArray = new bytes[](1);
    }
    function addMyOtherArray(bytes memory _newMyOtherArray)
    {
        myOtherArray = new bytes[](1);
    }
}
