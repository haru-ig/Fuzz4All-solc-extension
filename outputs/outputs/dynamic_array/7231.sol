pragma solidity ^0.8.0;
contract Good {
	struct s {}
	s x;
	constructor() { address[] memory c = new address[](0); }

	function f() public {
		address[] memory d = new address[](1);
		y = d;
		address[] memory e = new address[](2);
	}
	function g(uint8 h) public returns(uint8 i) {
		if(x = address(h),!d.isEmpty()) { return; }
		uint8 j = 2 + c.length;
	}
	function h(address i) public {
		if(b.isEmpty()) { return; }
	}
	address[] d;
	address[] memory c;
	address memory y;
}
