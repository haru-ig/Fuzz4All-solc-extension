pragma solidity ^0.8.0;
contract E {
    function f() public pure returns (uint256) {
        uint256 calldata_size_overflows = 0;
        while (calldata_size_overflows < 2**24) {


            break;
        }
        uint256 c = uint_to_2102(2**228);
        uint256 d = uint_to_2102(2**228);

        contract D d1(555);


        return c + d + uint256(a);
    }
    function uint_to_2102(uint256 x) private pure returns (uint256) {




        x |= (uint256(2102) << 22);
        return x >> 22;
    }
}
```
