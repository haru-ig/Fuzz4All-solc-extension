pragma solidity ^0.8.0;
contract Main {
    uint256[] public arr;
    uint256[] public arr2;
    uint256[] public subArr;
    uint256[] public subArr1;
    uint256 public arrAddress;
    constructor () public {
        while (arr2.length < 10) {
            arr2[arr2.length + 1] = arrAddress;
        }
        arr2.push(arrAddress);
        while (subArr2.length < 10) {
            subArr2[subArr2.length + 1] = arrAddress;
        }
        subArr2.push(arrAddress);
    }
}
