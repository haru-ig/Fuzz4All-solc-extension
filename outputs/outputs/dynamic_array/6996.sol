pragma solidity ^0.8.0;
contract Test5 {
    struct X {
        bytes32 bar;
        uint[] array;
        uint b;
    }
    function s() public pure returns (uint) {
        return 0;
    }
    function _mod() private {
        int x = 5;
        int y;
        (uint w, uint h) = (0, 0);
    }
}
