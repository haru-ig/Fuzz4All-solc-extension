pragma solidity ^0.8.0;
contract SemanticEquality_ArrayMutations394_2 {
    uint public i;
    uint x;
    function fct () public {
        i = 2;
        i[x].transfer(0);
    }
}


address theAddress;






import "./SemanticEquality_ArrayMutations393.sol";
import "./SemanticEquality_ArrayMutations394_2.sol";
address addr;
uint[] memory a;
SemanticEquality_ArrayMutations393 fct;
SemanticEquality_ArrayMutations394_2 fct_2;

addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);
a.length;
addr.transfer(theAddress);
a.length;
fct;

fct.fct(a);
fct.fct(a);
fct.fct(a);

addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);
addr.transfer(theAddress);

a.length;

addr.transfer(theAddress);

a.length;

a.length;
a.length;
a.length;
a.length;
a.length;
a.length;
