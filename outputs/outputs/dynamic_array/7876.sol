pragma solidity ^0.8.0;
contract Test10 {
    bool[] public foo;

    function f() public payable {
        unchecked {
            unchecked
        }
        bool[] memory b;
        address[] memory as;
        as.length = 0;
        foo.length = 1;
        foo.push(false);
        foo[0] = true;






        if(bytes32(foo[0])){

        }
    }
}
