pragma solidity ^0.8.0;
contract AA {
    uint[] mem;
}
contract AAA {
    uint[] mem;
}

pragma solidity ^0.8.0;
contract BBB {
    uint[] mem;
}
contract BBBBBB {
    uint[] mem;
}

contract A {
    mapping(address => uint) mem;
}

contract B {
    mapping(address => mapping(address => uint) mem);
}


contract C {
    mapping(address => uint) mem;
}

contract D {
    mapping(address => uint) mem;
}
contract E {
    mapping(address => uint) mem;
}
