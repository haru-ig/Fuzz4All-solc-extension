pragma solidity ^0.8.0;
contract C {
    address private c;
    address public D;
    uint[] private []privateX;
    uint[] [] public []publicY;
    function f() public {
        uint[][] memory y = new uint[4][];
        uint[][] memory z;
        C c;
        c.c.f();
        c.D.f();
        y[0] = new uint[](5);
        z = y;
        for (uint j = 0; j < 4; j++) {

            uint i;
            C x; y[j] = new uint[](5);
            for (i = 0; i < 5; i++) {

                uint n; Z[j][i] += 1 + n;
                n++;
            }
            return;
        }
    }
}
