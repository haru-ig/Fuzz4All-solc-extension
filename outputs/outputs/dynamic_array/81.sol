pragma solidity ^0.8.0;
contract A {
    uint256[] bArray;
    uint256[][] bArray2;
    uint256[][][1] bArray3;

    function doStuff() {
      uint256 temp = new SolidityAddress();
      bArray.push(new uint256[](1)[temp.slot]);
    }
}
