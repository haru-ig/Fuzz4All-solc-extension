pragma solidity ^0.8.0;
contract Test99999Overlapping {
    uint x;
    mapping (uint => uint) map;
    constructor() public {
        x.value(address(this).balance);
        var _0x = 2;
        x.value(map[_0x]);
    }
    fallback () external {
    }
}
