pragma solidity ^0.8.0;
contract Test31 {
    bytes32[] public ids;
    uint i = 500;
    constructor() public {
        ids.push("X");
        ids.push("Ya");
        ids.length = 4;
        ids.push(1);
        id.push("Y");
        ids.length++;
        ids.length++;
        ids.length++;
        ids.length++;
        ids.length++;
    }
}
