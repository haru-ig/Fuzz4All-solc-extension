pragma solidity ^0.8.0;
contract Test94C1 {
    struct A {
        uint256 array;
    }
    struct B {
        A array;
    }
    function f() public pure {
    }
    function g() public pure {
    }
    function h() public pure {
        g();
    }
    function i() public pure {
        if (i() > 0) {
            for (uint256 j = 0; j <= 5; j += 1) {
                uint256 k = j;
            }
        }
        for (uint256 j;
         j <=
         5;
         j += 1) {
        }
        if (h() > i() && (a() == 5) && ((a()[0]) + (a())[1])) {
        }

        if (
            (g() > (
                1 || (g() >=
                    1))) {
        }
        if (h() && (g() && (h() || (g() &&
            ((
              g() > (
                      1 || (g() >=
                          1))) && g() && (h() || (g() >=
                              1)))))) {
        }

        if ((
          g() < ((g() <=
                          1 ||
                          g() <
                          5) && g() >=
                          3))) {
            uint256 o;
        }
        if ((e() >
           (a() + (c()[g() - g() - c()])) +
           (b() + d()))) {
        }
        if ((e() <
           5)) {
        } else   {
        }
        uint32
            a = 5;
        uint55
            b = 5;
        uint32
            c = 5;
        uint256
            d;
        uint1
            e, f[1];
    }
    function a() public pure constant returns(uint32 ) {
        uint32
            x;
        /* 5
