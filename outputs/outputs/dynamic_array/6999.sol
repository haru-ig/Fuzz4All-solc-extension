pragma solidity ^0.8.0;
contract Test5 {
    struct Foo {
        uint128 foo;
    }
    function s() public pure returns (uint) {
        return 0;
    }
    function _mod() private {
        Foo f;
        (uint w, uint h) = (0, 0);
    }
}
