pragma solidity ^0.8.0;
contract EfficientDynamicArrayType {
    uint[] array;
    constructor() public {
        array.push(1);
    }
    function Test() public {
        uint256 y;
        for (uint i = 0; i < 1; ++i) {
        }
    }
}
