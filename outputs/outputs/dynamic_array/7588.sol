pragma solidity ^0.8.0;
contract Test20 {

    mapping (uint => uint) public A;

    constructor (uint A_,uint B_) public {
        A[A] = A_;

        B = B_;



        A[A] *= B;

    }




    event B(uint x);

    function b() public {
        emit B(B);
    }
}
