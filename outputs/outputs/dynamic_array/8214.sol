pragma solidity ^0.8.0;
contract FixedFabric {
    uint x;
    uint y;
    uint z;
}

pragma solidity ^0.8.0;
contract Test {
    mapping (uint => uint) z;
    mapping (uint =>uint) y;
   mapping (uint =>uint) x;
    mapping (uint =>uint) t;
    mapping(uint =>uint[]) t1;
    BrokenFabric b;
    FixedFabric f;
    uint i;
    uint [] k;
}
