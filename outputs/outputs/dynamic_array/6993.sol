pragma solidity ^0.8.0;
contract Test4 {
    struct Foo {
        bytes32 bar;
    }
    function s() public pure returns (uint) {
        return 0;
    }
    function _mod() private {
        int x = 5;
        int y;
        (uint w, uint h) = (0, 0);
    }
}
