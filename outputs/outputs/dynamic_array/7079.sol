pragma solidity ^0.8.0;
contract TestCaller{
constructor()public
{
}
}
```

A [Contract Upgradeability Specification (CAPEC)] (https:

This compiler will allow developers of smart contracts to work more closely with the language.  An upgrade will be triggered when a compiler flag is set in the compiler when compiling contracts (such as `--upgradeable`). This flag indicates that any changes in a contract's Solidity source code can trigger a smart contract upgrade (in addition to upgrades triggered by modifying the compiled bytecode).
