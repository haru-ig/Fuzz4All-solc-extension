pragma solidity ^0.8.0;
library mutantSolidity38941234290Library {
    event Change(uint indexed value);
    function change() pure private returns (uint) {
        Change(change());
        return 10;
    }
}
