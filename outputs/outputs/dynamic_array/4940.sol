pragma solidity ^0.8.0;
contract Test91b {
    address payable payed;
    uint value;
    uint witness;
    function foo(uint v) public payable {
        value = v;
        value;

        payout(msg.val);
        payout(payable(msg.sender));
        payout(0x0);
    }
    function bar() public view {
        foo(1);
    }
    function payout(uint v) private view {
        payed.transfer(v);
        emit payed(msg.sender, v);
        witness = msg.value;
        witness;
    }
    function get() public view {
        (address payable _addr) = payed;
        return _addr;
    }
    function set(address payable a) public {
        address w = a;
        w;
        w = address(0xffffffffffffffff);
        w;
        w;
        w;
    }
    function func(uint v) private {
        witness;
        witness;
        witness;
        (address payable) = payed0;
        (address payable _addr) = payed;
        _addr;
        value;
        value;
        (address payable payable0) = payed;
        (address payable) = payed;
        (uint) = ((uint(address(msg.sender)) >> 0));
        (address payable payable) = payed;
        (uint) = ((uint(((uint(((uint(address(this)) >> 0)) >> 0)) >> 0)));
    }
}
function test7(uint a) public pure {
    uint x;
    uint y;
    address a_;
    address b_;
    address c_;
    (a_, a_);
    (a_, a_);
    (a_, c_);
    (a_, a_);
    (a_, a_);
    (a_, a_);
    (a_, b_);
}
