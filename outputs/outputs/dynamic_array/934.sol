pragma solidity ^0.8.0;
contract NewSolidity {
    function pay(uint val) public payable returns (uint) { return 0; }
}

contract NewSolidity {
    function transfer(address dst, uint val) public returns (uint) { return 0; }
}
