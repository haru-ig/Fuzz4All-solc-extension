pragma solidity ^0.8.0;
contract Main {
    address[] arr;
    uint x = 0;
    constructor () public {
        uint y = 0;
        while ((x < y * z) + y > 852982) {
            y = y + 2;
        }
        arr.push(address(this));
        arr[0] = address(this);
        while (arr.length < x) {
            arr.push(address(this));
        }
    }
}
contract Main {
    mapping(address => bool) arr2;
    constructor () public {
        uint x = 0;
        bool y = true;
        while ((arr2[x]) + y > 852982) {
            y = y + false;
            x = x + 374;
        }
        arr2[arr2] = true;
        while (!arr2[x]) {
            x = x + 3543;
        }
    }
}
