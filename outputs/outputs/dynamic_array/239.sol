pragma solidity ^0.8.0;
contract C8 {
    struct T { uint256 s; }
    constructor(uint256 x) public {
        (uint256 mem, ) = (X == X? (X, T{}) == T{} : (T{}, T{}) == T{}? (T{}, T{}) : (X, T{}));
    }
}
