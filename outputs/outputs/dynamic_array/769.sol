pragma solidity ^0.8.0;
contract Test{
    uint public x = 42;
        function Test(){}
        function Test() { x = 24 * 24; }
        function Test() { x = 486; }
        bool greeter(uint[] calldata array) public pure returns(bool) { return(isSister(array) && isTrue(isTrue(isTrue(isTrue(isTrue(isTrue(isTrue(true)))))))); }
        function isSister(uint[] memory _xarr) public pure returns(bool) {
        uint x = 0;
        for(uint i = 0; i < _xarr.length; i++) {
            x = x + (_xarr[i] * (_xarr[i]));
        }
        return((x & 42) == 42);
        }
}
