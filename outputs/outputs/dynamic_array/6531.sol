pragma solidity ^0.8.0;
contract T8ES_mut_fixed {
    uint d = 0;
    mapping(uint256 => uint) m;
    constructor() public {
    }
    function add() public {
        address myaddr = address(100000);

        if(true)
            for(uint i = 0; i < 1; i++)
        ;
        else
            myaddr;
    }
}
