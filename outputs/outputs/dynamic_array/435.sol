pragma solidity ^0.8.0;
contract test7
{
    struct s2 {
        uint z;
    }
    s2[] private myArray;
    function modFunc() public {
        for(uint i=0; i<5; ++i){

        }
        uint z = myArray[i].z;
    }
}
