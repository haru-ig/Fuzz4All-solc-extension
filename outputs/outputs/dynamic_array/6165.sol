pragma solidity ^0.8.0;
 contract Test90008 {
    address[] a;
    constructor (address e, uint256 h) public {
        b1 = (8*e.balance);
        b1 += b1 + (10*h);
    }
    fallback () external {
    }
    event Mutate ();
}
