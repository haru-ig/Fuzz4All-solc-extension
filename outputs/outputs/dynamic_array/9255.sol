pragma solidity ^0.8.0;
contract semanticEquivalentMutation5B {
    struct MutatedArrayA {
     address x;
     bool[] isPresent;
     bool[] isNotPresent;
    }
    struct MutatedArrayB {
     uint256 x;
     bool isPresent;
     bool isNotPresent;
    }
    function semantic(uint256 maxItemsToMutate)
    public
    pure
    returns (uint)
    {
        address x;
        bool z;
        bool[] memory xx;
        bool[] memory xxx;
        uint zt;
        uint256 st;
        address y;
        uint zz;
        bool[] memory yy;
        uint256 zz2;
    }
}
