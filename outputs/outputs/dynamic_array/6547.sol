pragma solidity ^0.8.0;
contract T8ES_Mut5 {
    function a(uint idx) public {
        a(idx, 1);
    }
    function a(uint idx, uint size) public {
        uint z = 0;
        for(; z < size; z++) {

        }
        z++;
    }
}
pragma solidity ^0.8.0;
