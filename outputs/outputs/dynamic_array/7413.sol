pragma solidity ^0.8.0;
address payable p = {
    value:1000,
    gas:100000
};
function f(contract Test34 test){
    test.modifyTest();
    if (true && test.boolBool) {
        p.transfer(10 ether);
        p.transfer(10 ether);
        p.transfer(10 ether);
        p.transfer(10 ether);
        p.transfer(10 ether);
        p.transfer(10 ether);
    }
}
