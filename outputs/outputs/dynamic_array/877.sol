pragma solidity ^0.8.0;
contract WithDynamicArrays {
    function x() public pure returns (uint) {
        uint [] memory arr;
        arr[].length = 123456;
        uint x = addr;
        x = x + 150000;
        x = addr;
        return addr;
    }
}
