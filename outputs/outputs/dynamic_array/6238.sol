pragma solidity ^0.8.0;
interface I
{
    function foo() external returns (bool x);
}

pragma solidity ^0.8.0;
interface I
{
    function foo() external returns (bool x);
    function bar() external returns (bool y);
}
