pragma solidity ^0.8.0;
contract Test76 {
    enum TestEnum {A, B, C, D}
    event Emitted() public;
    fallback() nonpayable external;
    constructor() public {
        a[1] = bytes4(keccak256('emit()'));
    }
}
