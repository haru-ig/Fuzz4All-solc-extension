pragma solidity ^0.8.0;
contract test278 {
    uint a;
    uint b;
    uint c;
    uint h;
    function test(uint _c, uint _h) public {
        while (b >= _c && c < _h) {
            a += 1;
        }
    }
}
