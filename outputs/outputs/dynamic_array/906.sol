pragma solidity ^0.8.0;
contract NewSolidity {
    function x() public returns (uint256[] memory memoryX) {
        uint256[] memory memoryX;
    }
}
contract NewSolidity {
    mapping(bytes4 => bytes4) public x;
}
contract NewSolidity {
    mapping(bytes4 => function(uint) → bytes4) public f;
}
contract NewSolidity {
    mapping(bytes4 => bool) public x;
    function x() public {
        bool[] memory memoryX;
    }
}
contract NewSolidity {
    mapping(bytes4 => bool) public x;
    function x() public throws (bytes20, bytes20) {
        bool[] memory memoryX;
    }
}
contract NewSolidity {
    mapping(bytes4 => bool) public x;
    function x() public returns (uint1, bytes32[] memory memoryX) {
        uint1[] memory memoryX;
    }
}
contract NewSolidity {
    mapping(address => uint256) public map;
}
contract NewSolidity {
    mapping(address => uint) public map;
}
contract NewSolidity {
    mapping(bytes4 => address) public map;
}
contract NewSolidity {
    mapping(bytes4 => address) public map;
}
contract NewSolidity {
    mapping(address => uint) public map;
}
contract NewSolidity {
    mapping(address => bytes64) public map;
}
contract NewSolidity {
    mapping(address => bytes32) public map;
}
contract NewSolidity {
    mapping(address => bytes15) public map;
}
contract NewSolidity {
    mapping(address => bytes3) public map;
}
contract NewSolidity {
    mapping(address => bytes) public map;
}
contract NewSolidity {
    mapping(address => uint[]) public map;
}
contract NewSolidity {
    mapping(address => bytes22) public map;
}
contract NewSolidity {
    mapping(address => bytes1) public map;
}
contract NewSolidity {
    mapping(address => map[uint]) public map;
}
contract NewSolidity {
    mapping(address => map[bytes^2]) public map;
}
contract NewSolidity {
    mapping(address => map[bytes32]) public map;
}
contract NewSolidity {
    mapping(address => map[bytes8x]) public map;
}
contract NewSolidity {
    mapping(address => map[bytes8]) public map;
}
contract NewSolidity {
    mapping(address => map[bytes9]) public map;
}
contract
