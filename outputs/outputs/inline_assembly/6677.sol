pragma solidity ^0.8.0;
interface MixedContactsExample501Interface {
    function someOperation(uint256 x1) external returns (uint256 _x0);
}
contract MixedContactsExample501 {
    uint256 x;
    uint256 y;
    uint256 z;
    uint256 a0;
    uint256 b0;
    uint256 c0;
    uint256 d0;
    uint256 e0;
    uint256 a;
    uint256 b;
    uint256 c;
    uint256 d;
    uint256 e;
    uint256 y0;
    uint256 z0;
    event SoMEvent(uint256 value, uint256 count);
    function doSomething() public returns (uint256) {
        a = a0 + b0 + c0;
        b = a0 * 8;
        c = b0 + d0;
        a = a0 + 7;
        e = (a0 + y0) / (7 + (10 + 10 + 1 - x));
        a = a + (11 - (78 + 7 + u * (71)) / v * 2 * 90 - 7 * 54);
    }
    function doSomething2(uint256 x0) public returns (uint256) {
        bool y01 = (110 - y) % ((62 - x) + (7 - y) * (21 - (11 * (3 + v - u)) / x + 0.007590235) + 0.00082146315);
        uint256 z0 = (((x0 + x) - y0) / v / 21 % 31) + 46622;
        return a0 + b0 + c0 + d0 + e0;
    }
