pragma solidity ^0.8.0;
contract L8 {
        function sub2() public {
        uint256 a;
        assembly {

        a := x
        add(0x40, a, 0x3)
        }
        }
}
```
If a `call` instruction needs to have value, that value must be passed by the user in the call constructor.  */
