pragma solidity ^0.8.0;
contract MutatedContactsExample087 {
    uint x; x;
    uint y;
    uint z;
    uint y;
    uint(x);
    uint(z);
    constructor() {
        uint(y);
        uint(z);
        uint y;
        uint(y);
        uint(t);
    }
}

pragma solidity ^0.8.0;
contract MutatedContactsExample088 {
    uint(x);
    uint y;
    uint(y);
    uint(x);
    uint(z);
    constructor() {
        x;
        uint(t);
        x; uint(x);
        uint(t);
        uint(z);
        uint z;
        uint(z);
        uint y;
    }
}
