pragma solidity ^0.8.0;
contract Qb {
    uint constant Init = 2;
    uint rBb;
    constructor () public {
        rBb = Init;
        rBb += Init + Init + Init + Init + Init + Init + Init + Init + Init;
        uint n = Init;
        uint o = Init;
        uint q = 3000;
        for (uint x = 0; x < q; x++) {
            uint i = q + Init + Init + Init + Init + Init + Init + Init + Init;
            n = n + o + o;
        }
    }
}
