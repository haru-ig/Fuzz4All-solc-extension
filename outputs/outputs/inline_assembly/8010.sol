pragma solidity ^0.8.0;
contract MultipleOperators {
    uint x;
    uint y;
    uint z;
    uint w;
    uint8 d8;
    uint16 d16;
    uint32 d32;
    uint64 d64;
    uint128 d128;
    bool b;
    string s;
    bytes32 b32;
    address u;

    event Transfer(address indexed _from, address indexed _to, uint256 _value);
    function multipleOperators() public {
        x + = 1;
        y - = 3;
        x;
        x - y;
        w + w2 * 5;
        x + 3 ** 2 ^ (w * 128 + 5 + 9);
        y << d16 + 4 >> (3 + z >>> 9) + 5 ^ (x * z + d8) + 1;
        z %= b;
        uint j = x >>> i128;
        uint j2;
        if (x == x) {
            j2 = (b1_ || a1_ || b2_ || w!= a2_? x : b3);
        } else {
            j2 = x * x - (w!= b2_ && j!= (x + b3 + 6 ** (x * 128 - 1)));
        }
        d16 + y >>> d16 & 3 - y ^ c64 + (c32 + (x << 32 + (c8 + (c16 <= d32 + c32 + x64 - 128 * j - j ^ a8 + 6 * m8 * j) << 32) << 8 + (m16 + (d32 ^ m32 - y >>> d32 + w << 8 + c64) << 8 + (c32 + (c64 + (d64 + a64 + 9 ** (y >>>
