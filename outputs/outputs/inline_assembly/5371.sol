pragma solidity ^0.8.0;
contract QB {
    uint constant Init = 2;
    uint rAB;
    uint rBA;
    uint rABBA;
    constructor () public {
        rABBA = Init;
        rABBA -= Init - Init + Init + Init + Init + Init;
        for (uint x = 0; x < 1000; x++) {

            rABI := rABBA - rBA - rABBA + rABBA + rABBA;
            for (uint i = 0; i < 1000; i+=1) {
                uint j := rAB - rBA + rABBA + rABBA;
                rABBA = j + Init + Init;
                rABBA -= rABBA - rAB + rABBA + Init;
                rAB += rABBA;
            }
            rAB++;
            rAB++;
            rAB++;
            rAB++;
            rABBA -= rABBACA - Init;
            rABBA--;
            rABBA += rABBACA - Init;
            rABBA = rABBACA - Init;
        }
    }
}
