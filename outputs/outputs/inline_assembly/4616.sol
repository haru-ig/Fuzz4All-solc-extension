pragma solidity ^0.8.0;
contract emulator11 {
  uint x = 10000000000000000;
  uint y = 10000000000000000;
  function test1() public returns(uint) { x = 10000000000000000; y = 10000000000000000; uint a; for (uint i = 100; i < 400; i ++) {  uint c = x + 1; b; for (uint j = 100; j < 400; j ++) {
    c *= c + 1;
    for (uint k = 100; k < 400; k ++) { c += a * x; c += a * y; a = b;
    b = 100 + ( 1000 * c ) / c * (( c + 7 ) * ( c + 7 )) / ( c + 7) * ( c + 3 ) * ( c + 23 ) / ( c + 23) +
    (( c - 1 ) * c + 1 ) / ( c + 1 ) * ( 1 + 1 ) / ( c + 2 ) + b + a + j + k + i + 1 + 3 ); } } 2; c *= b * 10 * 10; x = c*a; y = c; 2; } } } }
