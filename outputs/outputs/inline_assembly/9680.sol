pragma solidity ^0.8.0;
contract SemanticYulContract {
 address developer;
    constructor(address newDeveloper) {
        developer = newDeveloper;
    }
    function addBy(uint x) internal {
        uint temp = c * 10 * 10 *10*5 *5 * x + x*5 * c  + 5;
        contractCounter += c* temp;
    }
}
