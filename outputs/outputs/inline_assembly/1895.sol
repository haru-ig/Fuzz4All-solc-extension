pragma solidity ^0.8.0;
contract newY {
    function func() private {
        uint256 max = uint256(1);
        uint256 x;
        uint256 x1;
        while (max >= 1) {
            x1 = x1 + max;
            max--;
        }
    }
}
