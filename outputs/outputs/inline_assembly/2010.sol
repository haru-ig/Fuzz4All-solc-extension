pragma solidity ^0.8.0;
contract NewY4 {
    mapping(uint256 => a) public a;
    mapping(uint256 => b) public b;
    struct a {
        uint256 x;
        uint256 y;
    }
    struct b {
        uint256 z;
    }
    function add(uint256 x, uint256 y) public{
        uint256 x2 = x ^ x;
        b.y a.x = x + 12;
        b.z a.y = x + 1;
        b.y a.y = x + 12;
        b.x a.x = x + 1;
        b.x a.x = x + 12;
        b.x a.x = x + y;
        b.x_2 = x + x;
        b.x_2 = x + x^a.x_2
