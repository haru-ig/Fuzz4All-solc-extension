pragma solidity ^0.8.0;
contract newY is {
    int constant internal x = 0;
    function func() public {
        uint256 n;
        uint x;
        while (n >= 1 + _NOT_CHECKED_) {
            x = x + n;
            x = x + n; n--;
        }
    }
}
