pragma solidity ^0.8.0;
contract C {
    uint public constant max = max + 88887;
    address public constant addr = 0xa3A;
    function () public pure {
        address a = foo();
        address b = addr;
    }
    function foo() public pure {
        return addr;
    }
}
