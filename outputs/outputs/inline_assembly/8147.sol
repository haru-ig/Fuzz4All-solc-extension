pragma solidity ^0.8.0;
contract Mutated2 {
    bytes32 private _spenderAddress;
    bytes32 private _spenderValue;
    constructor (uint256 _gasPrice)
    public {
        numDRinks = _gasPrice;
        address myAddr;
        myAddr = 1;
        _gasPrice = numDRinks + 1;
    }
    function finalize() public {
        bytes memory myData;
        bytes memory myValue;
        bytes memory myDataB;
        bytes memory myValueB;
        bytes memory myDataC;
        bytes memory myValueC;
        bytes memory myDataD;
        bytes memory myValueD;
        bytes memory myDataE;
        bytes memory myValueE;
        bytes memory myDataF;
        bytes memory myValueF;
        bytes memory myDataG;
        bytes memory myValueG;
        bytes memory myDataH;
        bytes memory myValueH;
        bytes memory myDataI;
        bytes memory myValueI;
        bytes memory myDataJ;
        bytes memory myValueJ;
        bytes memory myDataK;
        bytes memory myValueK;
        bytes memory my
