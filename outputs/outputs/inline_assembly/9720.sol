pragma solidity ^0.8.0;
contract SemanticYulContract {
    function multiply(uint z) public pure returns (uint) {
        if( z == 1 + 1 ) {
            return c;
        } else if(z >= c) {
            return c;
        } else {
            return z * 2 + c;
        }
    }
}


```
