pragma solidity ^0.8.0;

contract Solution5 {
    function test() public pure returns (uint) {
        uint y = 0x123;
        uint z = 0x456;

        uint subResult = 7 >= x * y;
        uint zPlusK = z + y;

        uint result = subResult + zPlusK;
        return result;
    }
}
