pragma solidity ^0.8.0;
contract Example{

contract Mutator{
    uint x;
    function add (uint _newX) public{
        x += _newX;
        }
}

pragma solidity ^0.8.0;
contract Example{

contract InlineExample{

    uint x;
    function addToX(uint _n1) public{
        x += _n1;
        }
}
```

<br/>

***[Back to top](https:
