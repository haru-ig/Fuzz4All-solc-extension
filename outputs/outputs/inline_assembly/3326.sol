pragma solidity ^0.8.0;
contract MyInlineAssembly{
  uint public value;
    data;
    constructor () {
        constructor(address(0));
      value = 0;
      }
      fallback() public payable {
        assembly {
      data := mload(0)
      value := add(value, mload(0))
      let b := byte(0, 65, data, 1)
      calldatacopy(3, 0)
      let x := and(mload(3), not(64))
      let s := add(mload(65, 1), mul(and(x, s1), eq(x, s1)))
      mstore(54, s)
      mstore(add(54, 2), mload(54))
      let f := sub(54, x)
    }
    }
}
