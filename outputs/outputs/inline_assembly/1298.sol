pragma solidity ^0.8.0;

pragma solidity ^0.8.0;
contract Mutant7a
{
    constructor() public {
      uint b;
      uint[] memory x;
      byte[] memory y;
      uint r;
      uint constant d = 719355519;
      bool v;
      (b, x, y, r, v) = (0, new uint[](0), new byte[](0), 0, true);
      assert(address(this).balance < d);
      assert(v);
      assert(address(this).balance == b + r);
      assert(false);
  }
  receive() external payable {}
}
