pragma solidity ^0.8.0;
import "@openzeppelin/contracts/token/ERC20/utils/ERC20Snapshot.sol";
contract MixedContactsExample98_v100 is ERC20Snapshot{
    uint256 public a;
    uint256 public b;
    uint256 public c;
    uint256 public d;
    uint256 public e;
    uint256 public z;
    uint256 public z_before;
    function doSomething() public {
        a = a * 0x62;
        b = b * 0x94;
        c = c * (uint256(ex));
        d = d * c;
        e = e * (uint256(ex));
        b = b - e;
    }
}
