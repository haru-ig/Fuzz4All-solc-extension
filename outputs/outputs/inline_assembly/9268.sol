pragma solidity ^0.8.0;
contract A {
    uint c = 10;
    function myFunction() public {
        c--;
    }
    function mutatedBy(uint) public pure returns(uint) {
        uint a = uint(_uint64) * 256;
        a += 32**32*(uint(_uint64) * 10) ** 32;
        return (uint(a - uint(uint(_uint64) * 256)) * 256) * 32;
    }
}
