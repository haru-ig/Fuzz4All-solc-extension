pragma solidity ^0.8.0;

contract A is B {
    uint namelength;
    uint names;
    constructor() {
        uint len = 3;
        uint name = 1;
        uint length = 3;
        uint names = 1;

        if(len > 1)
        {
        length = 3;
        }

    }
}

```
