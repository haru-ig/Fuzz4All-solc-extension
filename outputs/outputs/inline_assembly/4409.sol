pragma solidity ^0.8.0;
contract Emulator2 {
    function add(uint x, uint) public return(uint) {
        x = 5;
        uint ret = x-y;
        write();
        return ret;
    }
    function write() public pure {}
}
