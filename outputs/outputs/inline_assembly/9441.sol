pragma solidity ^0.8.0;
contract M {
  uint constant value = 5;
  uint counter;
  constructor() public payable {
    counter += value;
    counter --;
  }
}




address payable addr = address(msg.sender);
M.sol.counter.public;
M.sol.counter.public = 0;
msg.sender.balance;

address addr = 0x0101010101010101010101010109999;
M.sol.counter.public;
M.sol.counter.public = 0;
M.sol.counter.public;
address addr = addr;
addr.balance;

address payable caddr = address(this);
caddr.sendFunds(1);

contract M {
  uint constant value = 5;
  uint counter;
  fallback() external payable {
    counter += value;
    counter -= 10;
  }
}

contract M {
  uint constant value = 5;
  uint counter;
  constructor() public payable {
    counter += value + 10;
  }
}
```
