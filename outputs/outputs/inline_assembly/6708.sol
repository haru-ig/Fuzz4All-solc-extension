pragma solidity ^0.8.0;

contract MixedContactsExample499 {
    address addr1;
    address addr2;
    uint128 x;
    uint128 a;
    uint128 b;
    uint128 c;
    uint128 d;
    uint128 e;
    uint256 result;
    event SomeEvent(uint value, uint256 count);
    function doSomething(MixedContactsExample499 x) public returns (uint256) {
        addr1 = x.addr1;
        addr2 = x.addr2;
    if(addr1 == addr2) {
        a *= 8;
        b *= 68;
        bool x3 = x.x % 3 == 0;
        c *= 948;
        d *= 87;
        if(!x3){
            b += 5;
        }
        e *= 7;
        a += c + b ;
    }
        result /= 60;
    }
}
