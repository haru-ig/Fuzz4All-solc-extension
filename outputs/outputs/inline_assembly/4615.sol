pragma solidity ^0.8.0;
contract emulator11 is emulator10 {
  function test1() public returns(uint) { uint a; for (uint i = 100; i < 4000; i ++) { a = a * 1000 + 100; }
  z = z * 1000 + a; }
  function test3() public returns(uint) { z = z * 1000; uint a; for (uint i = 100; i < 4000; i ++) { a = a * 1000 + 100; }
  z = z * 1000 + a; }
  }



pragma solidity ^0.8.0;
contract tester {
  function test10() public returns(uint) { uint a; for (uint i = 100; i < 4000; i ++) { a = a * 1000 + 100; }
  z = a; }
  function test11() public returns(uint) { uint a; for (uint i = 100; i < 4000; i ++) { a = a * 1000 + 100; }
  z = a; }

  }
