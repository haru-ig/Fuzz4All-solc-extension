pragma solidity ^0.8.0;
contract emulator12 {
  uint z = 100000000000000000;
  uint  a;
  function test1() public returns(uint) { for (uint i = 100; i < 4000; i ++) { a = a * 1000 + 100; }
  z = z * 1000 + a; }
    z = z * 10000 + a * 100000 + a*1000 + a; }
   }
