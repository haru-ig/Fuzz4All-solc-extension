pragma solidity ^0.8.0;
contract MixedContactsExample502 {
    uint56 x;
    uint56[4] b;
    uint56 a;
    uint56 y;
    event SOMEVENT(uint value, address indexed recipient, uint64 count);
    constructor () {
        x = 9<fim_suffix>;
            uint56 val = b[11];
            uint56 val_2 = b[12];

                b[4] = x + val_2 * val;

                b[7] = ( x^ val_2) | ( val_2 ^ (val + 40 ) );

                b[10] = x - x * val_2 ;

                b[8] = ( (x >>> 5) ^ val) & (val ^ (val == 15 ));

                b[9] = (x^val) | ( val ^ a);

                emit SOMEVENT(x, msg.sender, val);


        } else {

            uint56 x_i = x + b[4];

            b[7] ^= x_i * x_i;

            x = ( x_i * x_i ) >> 5 + x_i ** 32 - x;

            bool val = x > b[9];


                b[9] += (x ^ val) ^ ((x ^ val) << 24);

                b[7] ^= x*x ;

                b[4] = x^x_i;



        }

        emit SoMEvent(x, b[7]);
    }
}
