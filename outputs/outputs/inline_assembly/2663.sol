pragma solidity ^0.8.0;
contract C {
    uint public a;
    uint public x;
    uint ky;
    uint w;


    constructor () public {
        x = 3 * z - x / 4;
        a = 300 + a + a + a + a + a + x + y;
        w = a;

    }

    receive () external {

        a = 300 + a + a + a + a + a + x + y;
        w = a;

    }
}
