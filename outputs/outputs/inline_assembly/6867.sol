pragma solidity ^0.8.0;
contract MutatedContactsExample087B {
    uint public test;
    uint x;
    uint y;
    uint t;
    constructor() {
        uint(t);
        uint(x);
        uint(test);
        uint(t);
        uint(x);
        uint(t);
        uint(test);
        uint(t);
        uint(x);
        uint(test);
        uint(t);
        uint(x);
    }
}
