pragma solidity ^0.8.0;


contract Example
{
    function add() public {
        uint storage local_x = 5;
        uint storage local_y = 5;
        uint x = x = x + x;
        local_x = x + x + x;
        local_y = x + x + x + local_y;
    }
}
