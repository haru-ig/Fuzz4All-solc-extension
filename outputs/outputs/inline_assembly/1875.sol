pragma solidity ^0.8.0;
contract newZ {
    uint internal n;
    function func() public {
        uint256 num;
        while (num >= 1) {
            string memory str;
            byte[] b;
            byte[] b2;
            str = str.substr(4);
            num = num - 2;
            num = num - 2;
            num = num - 2;
        }
    }
}
