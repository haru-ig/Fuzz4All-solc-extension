pragma solidity ^0.8.0;
contract newY {
    int constant internal x = 0;
    function func() public pure {
        uint y;
        uint x = 0;

        while (y <= 1) {
            x = x + y * max;
            max--;
        }
    }
}
