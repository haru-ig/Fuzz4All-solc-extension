pragma solidity ^0.8.0;
contract NewY4 {
    uint8 constant _K = 0;
    uint256 private constant _N = type(uint256).max / type(uint8).max;
    uint256[internal _N][_N] public a;
    uint8[internal _N] public b;
    function add(uint256 x, uint256 y) public{
        if (x!= 0){ x = x + 1;}
        if (a.length > 0) {
            uint256 b_y=b[x];
            type(uint256).max = a.length;
            type(uint256).max = b_y;
        }
        type(uint256).max = 6;
        type(uint256).max = b_y;
        type(uint256).max = 7;
        type(uint25
