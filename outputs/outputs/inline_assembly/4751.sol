pragma solidity ^0.8.0;
contract mutator120 {
    bool isInitialized;
    uint256 h;
    uint8 w;
}

pragma solidity ^0.8.0;
pragma solidity ^0.8.0;
contract mutator120 {
    uint256 h;
    bool isInitialized;
    uint8 w;
    constructor() { if( 0x0d26ef3690048da923c1c7ec34812d0b692227da isInitialized) isInitialized = true; }
}

pragmatally solidity ^0.8.0;
contract mutator120 {
    uint256 h;
    uint8[128] memory mem;
    bool isInitialized;
    uint8 w;
    constructor() { h = 0x1288;
         w = 6;
    }
}
/* Please create a program that can be executed on any Ethereum node that has
Solidity.

Note that this program should work either locally or remotely.
Note also that the program is not required to have an EVM - it could even be
implemented on a smart contract just like this one. However, I would discourage implementing
a program on a smart contracts as this makes any change to the contract a potentially
