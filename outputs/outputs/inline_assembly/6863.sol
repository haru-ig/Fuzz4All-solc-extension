pragma solidity ^0.8.0;
contract EvolvableContactsExample087 {
    uint public constant test = 69;
    uint x;
    uint y;
    uint z;
    uint t;
    constructor() {
        x;
        uint(t);
        x;
        uint(z);
        uint(x);
    }
}

pragma solidity ^0.8.0 ;
contract MutatedContactsExample088 {
    uint public constant test = 111;
    uint x;
    uint y;

    constructor() {
        uint(t);
        x;
        uint(t);
        uint(z);
        uint(x);
        uint(t);
    }
}
