pragma solidity ^0.8.0;
contract MutatedContactsExample087 {
    uint public test;
    uint x;
    uint y;
    uint t;
    constructor() {
        uint(test);
        uint(t);
        uint(x);
        uint(test);
        uint(t);
        uint(x);
        uint(t);
    }
}
