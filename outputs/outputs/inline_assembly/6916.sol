pragma solidity ^0.8.0;
contract OptimizelyExample86 {
    uint number = 2;
    uint constant number = 8 / 7;
    uint constant number = (4 * 4) + 2;
    uint constant number;
    uint constant number = 16 / (3 * 2) + 2;
    uint constant number = (4 * 4) + (2 * 2) - 2;
    uint number = -2;
    uint number = (2 * 2);
    uint constant number = number + (4 * number);
    uint constant number = number + ((4 * number) + number);
