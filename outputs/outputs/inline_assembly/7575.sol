pragma solidity ^0.8.0;
contract Example {
    uint constant initialx = 0;
    uint constant initialy = 0x1234;
   function f() public returns (uint) {
       uint = 0 + 42;
       uint = _tmp1 + 0x1234;
       uint = initialy + 0x1234 + initialy;
       uint = initialy + _tmp3 + initialx;
       uint = initialy + _tmp1 + f();
       uint = _tmp7 + initialx;
       uint = initialx + _tmp6;
     return initialx + _tmp5 + initialy;
   }
}
