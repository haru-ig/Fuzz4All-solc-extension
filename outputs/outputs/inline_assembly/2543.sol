pragma solidity ^0.8.0;

contract Comp34{
        constructor(uint32 _initialAmount) {
          a = _initialAmount;
        }
function add(uint32 _b) public{
    a = a + _b;
}

pragma solidity ^0.8.0;
uint64 constant _ONE = 1;
uint128 constant _ONE_HUNDRED = 10 ** 6;
uint32 constant _EIGHTEEN = 15;

function square(uint256 _a) pure public returns (uint256 b) {

b = _a / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED /_ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED /_ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED /_ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE_HUNDRED / _ONE
    unchecked {
        b = _a / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE / _ONE /_ONE / _ONE / _ONE / _ONE / _ONE /_ONE / _ONE / _ONE / _ONE / _ONE;
    }
}
contract Comp34{
        uint32 a;
        constructor(uint32 _initialAmount) {
            a = _initialAmount;
        }

pragma solidity ^0.8.0;

library Opcodes_Lib{
    function add(uint a,uint b) pure returns(uint c){
        unchecked {
            c = a +b;
        }
    }
}
uint c;
uint i;
uint b;
constructor(){
    b = 2;
}
function add(uint _i, uint _b) public{
    c = Opcodes_Lib.add(_i,_b);
}
/*Please create a contract
