pragma solidity ^0.8.0;
contract game {   function run () internal view returns (uint x) { uint start = z; x = start + 1; z = z + 1;
    x = z * x; x = z * x; x = z * x; x = z * x; x = z * x; x = z * x; x = z * x;
    x = z * x; x = z * x; x = z * x; x = z * x; x = z * x; x = z * x; x = z * x;
    z = x; x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    x = (x + 1) * (x - 1); z = z * (z + 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    for (uint i = 0; i < 10000; i ++) { x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    x = (x + 1) * (x - 1); x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    for (uint j = 0; j < 10000; j ++) { z = (z * (z + 1)) + 1; x = (x + 1) * (x - 1); x = (x + 1) * (x - 1);
    x = (x + 1) * (x - 1); x = (x + 1) * (x -
