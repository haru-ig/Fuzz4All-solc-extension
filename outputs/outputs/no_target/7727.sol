pragma solidity ^0.8.0;
contract Foo {
    function barmut() public pure returns (bool x_) {
        (bool x, bool y) = (address(this), false);
        bool z = x_;
        return z;
    }
    function bar() public pure returns (bool x_) {
        (bool x, bool y) = (address(this), false);
        return z;
    }
}
