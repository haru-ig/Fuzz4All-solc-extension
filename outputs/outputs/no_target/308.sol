pragma solidity ^0.8.0;
contract MutatedMutatedMutatedMutated {
    function _mutated() internal pure returns (uint) {
        return ((10+10)+(10+10));
    }
    uint public mutated _previous;
    function balance() public view returns (uint) {
        _previous + _previous + _previous + _previous * _previous + _previous * _previous * _previous + _previous * _previous * _previous * _previous;
    }
