pragma solidity ^0.8.0;
contract C {
    uint private v;
    function myFunction() public {
        uint256 n;

        v = n;
        v = uint256(uint128(v) * 0x100;
    }
}
contract C {
    uint private v;
    function myFunction() public {
        uint256 n;
        v = n;
        v = uint256(uint128(v) >> 128);
    }
}
