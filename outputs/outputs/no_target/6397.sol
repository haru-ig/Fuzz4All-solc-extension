pragma solidity ^0.8.0;
contract mutatedE {
  function m(uint8 input) public returns (uint8) {
    return input;
  }
}
```
# 0.7.0 Release notes

## Compiler and language changes

* The compiler has been revised and upgraded to Sourcify. As a result of the upgrade to Sourcify, only one compiler change is required to integrate the new version of the compiler with the rest of the software stack.
