pragma solidity ^0.8.0;
contract seven {
    uint public a;
    uint public b;
    function __func__() public {
        a = (b + r - 21 - a) / 36;
    }
}

pragma solidity ^0.8.0;
contract eight {
    uint public a;
    uint public b;
    function __func__() public {
        b = a * (b - b + 7 - b) / (a / b + 1 / b);
    }
}
