pragma solidity ^0.8.0;

library UintWrapper {

    uint constant X = 1;
    uint a;
    function x() pure public returns (uint) {
        if (a < 2) {
            if (a < 2) {

            }


            return 1;
        } else {
            a += 1;


            return 1;
        }
    }
}
