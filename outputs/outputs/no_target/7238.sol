pragma solidity ^0.8.0;
contract UpgradableCounter {
    uint x = 1;
    uint internal y = 0;
    uint public constant max = 5;
  function Mutate() public {
     while(true) {
        if(y >= max) {
          unchecked {
            y -= unchecked(733);
           y = x - 456;
            y += unchecked(546);
            y -= unchecked(236);
           y -= unchecked(53246);
          }
           y += unchecked(556);
            y -= unchecked(170);
            y += unchecked(905);
            y += unchecked(490);
            y -= unchecked(34);
            y += unchecked(99);
           y -= unchecked(48459);
            y += unchecked(5366);
            y -= unchecked(733);
           y -= unchecked(845);
            y += unchecked(26536);
            y += unchecked(35);
           y -= unchecked(495);
            y -= unchecked(12452);
            y -= unchecked(435);
           y -= unchecked(5467
