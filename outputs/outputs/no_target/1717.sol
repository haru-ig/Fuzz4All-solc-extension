pragma solidity ^0.8.0;
contract Mutate0015b {
    function multiply() public pure returns (uint) {
        uint256 bool_input = false;
        if (bool_input)
            return uint(1);
        else
            return uint(0);
    }
}
