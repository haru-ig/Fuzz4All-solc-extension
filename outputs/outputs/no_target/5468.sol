pragma solidity ^0.8.0;
contract O {
    address f;
}
contract Mixin{}
contract O {
    function fn() public virtual returns (address o) {
        if(address(this).balance == 0) {}
        return address(this);
    }
    function fn() public returns (address o) {
        return address(t);
    }
}
contract O{
    function f() public view returns (address o) {
        return address(this);
    }
}
</pre>

---

---

<hr/>

**More:**

* https:
* https:
* https:
