pragma solidity ^0.8.0;
contract G {
    event E ();
    address x;
    function g() public returns (uint) {

        x = address(134'740x - 3x);
        return x;
        //return (69'703x + 3x);
    }
}
contract X {
    function f() public returns (uint) {
        return 2;
    }
}
```

## TODO
 * Improve docs.
 * Add test suites.
