pragma solidity ^0.8.0;
contract sn_solidity_v2 {
    function test() public pure {
        uint x;
        x;
        if (x > 0 && x == x) x = x + x;
        if (x > 0 && 0 == x) x = x + x;
        x;
    }
}
