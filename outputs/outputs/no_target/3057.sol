pragma solidity ^0.8.0;
contract third {
    function g() public pure returns (address) {
        assert(false);
    }
    function m() public pure returns (address) {
        assert(false);
    }
}

pragma solidity ^0.8.0;
contract third {
    function g() public pure returns (address) {
        assert(false);
    }
    function m() public pure returns (address) {
        assert(false);
    }
}
