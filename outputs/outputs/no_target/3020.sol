pragma solidity ^0.8.0;
contract third {
    function a(uint c ) public pure {
    uint x = -3000;
    uint y = -3000;
    uint z = x + y;
    uint w = x - y;
    uint8 y2 = x / z;
    uint8 z2 = z / x;
    uint8 zw = z ^ x;
    uint8 l = c - 1;
    uint x8 = unchecked((uint8((uint64(l) << 8) - x)));
    uint x88 = unchecked(uint88(l - x8) / uint88(z2) >> 8);
    uint64 zz = x88 < x8? 1 : 0;
    zz ^= (z ^ w) + (w >> 256);
    zz *= zz;
    bool b = false;
    uint x1 = 5 / uint(-8);
    uint x11 = x1 & 10000000000000000000;
    uint x111 = uint(x11) / 40000000000000000000;
    uint x2 = 128;
    uint x22 = x2 << 8;
    uint x222 = x22 << 16;
    uint x23 = uint(x22 >>> 16) * uint(x23);
    uint x233 = uint(x222) + uint(x23) + uint(x2) / 4;
    uint160 x37 = uint160(x2) / uint160(x223);
    uint160 x38 = x37 / uint160(uint160(x1) & 10000000000000000000);
    uint32 x42 = uint32(x1 & 10000000000000000000) / uint32(x2 - x);
    uint a = uint8(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint(uint88 >> 16) + ((uint64(uint(uint160(x2) | (((uint64(x38) | zz) << ((uint64(uint160(x2) | zz) << uint88)) << 16))) >> 48)) | 0 - 128)) | zz) << ((uint64(uint160(x2) | zz) << ((uint64(uint160
