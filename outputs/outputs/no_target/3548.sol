pragma solidity ^0.8.0;
contract m207 {
    uint constant MAX = 1 << 160;
    constructor() public {
        uint x;
        x = s;
        x = x.add(s);
        x += s;
        x -= s;
        ((x.sub(s)));
        x = x * s;
        x = s ^ s;
        x = x << 1 + s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s = s >> s;
        s++;
        s <<= 1;
        s <<= 1;
        s <<= 1;
        s <<= 1;
        s <<= 1;
        s <<= 1;
        s = s >> s;
        s >>= 1;
        s >>= 1;
        s >>= 1;
        s >>= 1;
        s >>= 1;
        s >>= 1;
        s >>= 1;
        s = s >> s;

        uint y;
        y = s;
        y = y - 888;
        y = 56 * 7;
        y = y * y;
        y = y >> y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        y = y >> 1 * y;
        f(y);

        y = s.add(y);
        y = y.mod(s);
        y = y.add(y);
        y = y - 1 + s;
        y = y - 1 + s;
        y = s.mul(y);
        y = y >> y.add(s);
        y = s.f(y + 1);
        y = y * y;
    }
    function s(MyStruct memory o) public pure returns (uint) {
        return o.x;
    }
    uint constant f(uint w) internal view returns (uint) {
        return w.div(2) << 2;
    }
    unchecked
    function x() public pure returns (uint) {
       return 1;
    }
}
/* Please create a
