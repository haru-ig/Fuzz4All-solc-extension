pragma solidity ^0.8.0;
contract O {
    mapping(address=>uint) public y;
    uint y0;
    function f() public returns (uint) {
        uint x = 17;
        uint y00010 = 1**100;
        uint y0M1 = UINT160(type(uint160).max);
        uint y0000 = 1;
        uint y0_ = y0_;
        uint y01 = type(uint160).max-type(uint160).max+type(uint160).max+1;
        uint y_ = uint160(y0);
        return((x-(x-(x-x)/x)));
    }
}
