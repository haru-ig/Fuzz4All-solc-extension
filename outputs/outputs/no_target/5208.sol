pragma solidity ^0.8.0;
contract SimpleExample15 {
    bool x;
    constructor() public {
        x=true;
    }
    function doSomething(address addr) public {
        if(x) {
            revert('Check');
        }

        do {

        } while(address(uint160(0x1a2b)^{uint512(0x1a2b)}<addr));
        (uint _value;
        unchecked {
            _value = uint80(bool(true));
        });
    }
}
