pragma solidity ^0.8.0;
contract EscapedConstructor4 {
    uint s;
    uint i;

    address a;
    uint p_a;
    address a0;
    constructor () public {
        i = x128 = 1 + x128 * 1;
        if (s > 0) i += 1;
        a0 = a;
    }
    modifier m0() {
        return;
    }

    function f() public view returns(uint, uint) {
        uint s;
        s = p_a;
        return (x - 128 + s, x_ - s) * 1 / 128;
    }

    function g() public pure {
        uint s;
        s = p_a;
        s = i + s / 128 + i + 1 / 128 + 1;
        s >>= 256;
        s = x - s;
        x_;
    }

    function t() public pure {
        uint s;
        s = p_a;
        i;
        i = a - s;
        uint x;
        x = s + i + i / 128 + 1 / 128 + 1;
        x >>= 256;
        x = x + s;
        x = x * 1 / 128;
        x;
    }
}
