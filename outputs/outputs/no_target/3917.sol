pragma solidity ^0.8.0;
 contract C {
   uint x = 0x7FFFFFFFFFFFFFFF;
   uint x2 = 0x1FFFFFFFFFFFFFFF;

   uint y;

   function f() public {
       y = int(x+x2);
   }
}

contract C {
    bytes32 _bytes32;
    mapping (address => mapping (address => uint)) public _map[uint160](address) [uint111];
}
contract C {
    bytes32 _bytes32;
    mapping (address => bytes32) public _map;
}
