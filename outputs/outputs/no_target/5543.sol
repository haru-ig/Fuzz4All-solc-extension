pragma solidity ^0.8.0;
contract O {
    mapping(uint=>bool) public z;
    function f(uint y) public returns (bool) {
        z[(y)-(y)] = true;
        uint x = 1;
        uint z = (x-(x-x));
        y = y*y+y;
        return z;
    }
}

pragma solidity ^0.8.0;
contract O {
    function f(address address) public returns (bool) {
        uint x = 1;
        uint z = (x-(x-x));
        return z;
    }
}

pragma solidity ^0.8.0;
contract O {
    function f(address,address,address) public returns (bool) {
        uint x = 1;
        uint z = (x-(x-x));
        x = (x-x)*(x-x);}
}
