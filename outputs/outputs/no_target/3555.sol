pragma solidity ^0.8.0;
contract m207_2 {
    struct MyStruct {
        uint x;
    }
    MyStruct public s;
    uint public i;
    constructor(uint b, uint b0, uint b1) public {
        s = s;
        i++;
        i += b + b0 + b1;
        if (4!= 3) revert();
        s.x = s.y + 10;
        s.y = s.z + 16;
        delete s.z;
    }
}
