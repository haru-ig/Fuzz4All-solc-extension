pragma solidity ^0.8.0;
contract A {
    uint24 d;
    uint value;
    function g() public {
        d -= value;
    }
}

pragma solidity ^0.8.0;
contract A {
    uint d;
    uint d;
}
pragma solidity ^0.8.0;
contract A {
    uint d;
    uint24 d;
}
contract A {
    uint d;
    uint value;
    uint x;
    uint y;
    uint z;
    function zzzz(uint24 aaa, uint24 bbb) public { d *= aaa; }
    function g() public returns (uint) { return uint(-x); }
    function h() public returns (uint) { return uint(3); }
    function i() public returns (uint) { return uint(16 + 3 + uint(123)); }
    function j() public returns (uint) { return uint(uint(3 + 84 + 255)); }
    function k() public returns (uint) { return uint(7 * 3 + 99); }
    function l() public returns (uint) { return uint(5 ^ x + y); }
    function m() public returns (uint) { return uint(x & y & z & 3); }
    function n() public returns (uint) { return uint(aaa + x + y + z); }
}
contract A {
    bytes1 d;
    uint32 v;
    uint64 x;
    uint24 y;

    function hhhh() public returns (uint) { return uint(16 + 3 + uint(123)); }
    function g() public returns (uint) { return uint(uint(3 + 84 + 255)); }
    function i() public returns (uint) { return uint(6 ^ -2 + 2) / 3 + 5; }
    function j() public returns (uint) { return uint(uint(3 + 84 + 255)); }
    function k() public returns (uint) { return uint(y ^ x & 7 * 3 + 99); }
    function l() public returns (uint) { return uint(x & x & x & x & 0xFF).
