pragma solidity ^0.8.0;
contract TestMutated731 {
    function mul(uint x, uint) public pure returns (uint) {
        if (0x55555555 < 0) {
            return mul(7x, 890u);
        }
        uint z;
        return z;
    }
}

contract TestMutatedN736 {
    function mul(uint, uint y) public pure returns (uint) {
        uint z;
        return z;
    }
}
contract TestMutatedN737 {
    function mul() public pure returns (uint) {
        uint z;
        return z;
    }
}
contract T {
    function test() public pure returns (uint) {
        return (uint(2) + 2) * (uint(2) / 3);
    }
}
