pragma solidity ^0.8.0;
contract O {
    mapping(address=>uint) public y;
    function f() public returns (uint) {
        uint x = 1;
        if((((x-(x-x))-(x-(x-x))));
        return (((x-(x-x)))-(x-(x-x))));
    }
}

pragma solidity ^0.8.0;
contract O {
    mapping(address=>uint) public  y;
    function f() public returns (uint) {
        uint x = 1;
        (uint y=(x-(x-x)))-(x-(x-x));return (((x-(x-x)))-(x-(x-x))));
    }
}
