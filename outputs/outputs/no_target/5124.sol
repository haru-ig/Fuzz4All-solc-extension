pragma solidity ^0.8.0;
contract ___mutated_previously_generated_contract {
    using __mutated_previously_generated_contract for address;
    function test() public pure {

        address a = C___mutated_previously_generated_contract(1).balance;
        function() { revert(0); }();
        address b = address(this);
        address c = address(this);
        b.delegatecall(abi.encodeWithSignature("()"););
    }
}
