pragma solidity ^0.8.0;
contract K {
  uint z;
  constructor() public { z; }
  function f() public {
    z + x; uint w; w^=x; w + x; z^=x;
    z; w > y;
    z; x + 0;
    z & (y < (y > x) - y); uint y;
    z * x;
    z & (z < x > x);
    uint z; x - x;
    uint x; x -= x + x;
    uint x; x += -x + 0;
    z = 1; uint x; uint y;
    uint x; x += x > y + 0;
    z; uint y; y++; y--; z; uint z; x; z = 1;
  }
}
