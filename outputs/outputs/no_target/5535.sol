pragma solidity ^0.8.0;
contract O {
    address f(uint x)
        public {
        return y[x^x];
    }
    function f() public returns (uint) {
        uint x = 1;
        return ((x^x)-(x^x));
    }
}

```
