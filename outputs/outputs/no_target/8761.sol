pragma solidity ^0.8.0;
contract EquivalentABIv08AbiV1SemanticallyEquivalentInterface {
    function isEqualTo(address x, address y) public view returns (bool);
    function mint(address addr) public returns (bool);
}
