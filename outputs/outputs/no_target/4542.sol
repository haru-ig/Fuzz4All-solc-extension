pragma solidity ^0.8.0;
contract C {
    address private owner;
    bytes32 private v;
    function myFunction(bytes32 num) public {
        v = num;
        function call(bytes32 num) public {
        }
    }
}
