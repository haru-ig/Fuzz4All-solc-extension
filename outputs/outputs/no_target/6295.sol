pragma solidity ^0.8.0;
contract MyContract {
    function get(address a) public {
        address addr = address(0);
    }
    function set(address a) public {
        address addr = address(0);
    }
}
