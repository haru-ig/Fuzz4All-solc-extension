pragma solidity ^0.8.0;
contract SimpleExample21 {
    constructor() public {
        num < 2 && num > 0 || 1 <= num <= 4 / 5;
        uint x;
        uint y;
        uint z;
        uint w;
        num = num + 35 / 5 || num + 3 * (3 / 4) + 5 / y;
        num * 4 / 5 || num / 3 / 2 && num - 2;
        num / num + 1;
        num / num - 1;
        num**4;
        num || num > num - 1;
        num <= num && num >= num + 1;
        num & (num + 1);
        num & 0xABCD;
        num < 0x8000000000000000;
        num * 1 / 4 && num / 2 / 5;
    }
    uint256 public num;
}
