pragma solidity ^0.8.0;
contract A {
    address payable addr;
    constructor() {
        emit A(add.value(42)(addr));
    }
    function addValueTo(address a) public {
        emit A(add(a));
    }
}

pragma solidity ^0.6.0;
contract NotModified {
}
