pragma solidity ^0.8.0;
contract O {
    uint constant x = 1;
    uint constant y =  2;
    function g(uint a, uint b) internal {
        uint8 c = (uint8)(b >> 8);
        c * b;
        c = b + c;
        c + c;
      !b;
        c >> 16;
        uint constant x28 = (uint28)(b);
        a & 256;
        uint constant x39 = (uint39)(b);
        uint256 c9 = b + 20;
        c5 >> 8;
        c = 2 * c;
        c9 || 1 / 0;
        c <= true;
        x + c;
        uint8 aa;
        aa -= 3;
        aa >= a;
        if(a <= aa) b >>= c1;
        true && uint(-1 - b * 5) & (a > c);
        uint64 cc9;
        uint cc9d = (uint(-1) - (uint32)(b) % 13);
        a == -1;
        255 / uint192;
        (uint256 - 3) < 0 || addressof(O);
        void(uint96) ^ uint64(address(this));
    }

    function h(uint a, uint b) internal {

    }
}
