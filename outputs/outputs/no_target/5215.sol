pragma solidity ^0.8.0;
contract SimpleExample17 {
    function doSomething(bytes memory _data) public {
        unchecked {
            num = num ^ 1;
            assert(num == uint(uint256(uint160(0x3900))));
        }
    }
}
