pragma solidity ^0.8.0;
interface i {
    function m() external returns (uint256 i);
    function m12(uint32 x, uint256 y) external returns (uint256);
}

interface i {
    function m() external payable returns (uint256 i);
}
