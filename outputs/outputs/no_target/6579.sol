pragma solidity ^0.8.0;
contract myContractS {
    function h() public pure returns(uint) {

        if (msg.data.length > 65535){


        myContractU c = new myContractU();
        return (uint) c.g(1);
        }
    }
}
