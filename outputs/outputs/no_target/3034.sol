pragma solidity ^0.8.0;
pragma solidity >=0.8.0;
contract third {
    function g() public pure {
        address a = address(1);
    }
    function h() public pure {
        address a = address(0);
    }
}
