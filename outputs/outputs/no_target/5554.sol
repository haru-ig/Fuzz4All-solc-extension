pragma solidity ^0.8.0;
contract O {
    mapping(address=>uint) public y;
    function f() public returns (uint) {
        int x = -1;
        uint z = (x/(x+z));
        uint y = x+x;
        y = z%3;
        y = z%3;
        return y;
    }
}
