pragma solidity ^0.8.0;
interface f98_1 {
    function t() external pure returns(int64);
}

pragma solidity ^0.8.0;
contract b4e50 {
    function showMessage(string memory msg) public view returns (string memory);
    function noInlineFunction() public pure returns(address);
    function noInlineFunction() public pure returns(int32);
    function showMessageReturn1(string memory msg) public pure returns (string memory);
}

pragma solidity ^0.8.0;
contract 3ae47 {
    function f1() public return (uint256 a) {
        address addr = msg.sender;
       a = 0x42ea3400;
       if (a < 0) {
        a *= 0 + 1;
       }
       uint256 b = 0x1d03;
       if (b < 32) {
        uint256 c = 0x1d03;
       }
       uint256 z = a * 4294967296;
       uint256 w = a / 4294967296;
       uint256 x = b / b;
       if (z!= b * b) {
        w + (b & 0x0400040004000400) / x;
       }
       uint256 y = uint256(2).log10() - 20;
       if (z == 0x4000200020002000) {
        a += 3;
       }
       if (b == uint256(-1291)) {
        a += w * (a - a / 3);
       }
       uint256 y_ = uint256(1).log10() + 10;
       uint256 v_ = a - b * (2 ** (uint256(1).sqrt() * 20));
       uint256 x_ = y_ - y;
       if (v_ % 9 == 0) {
        if (y < b) {
         a *= x_ - x_;
        }
       }
       if (y < 1) {
        x += x_;
       }
       return a;
    }
    function f8() public pure returns(uint256 a) {
       a = 100;
       a = a + 3;
       a = a / 10;
       a = a - b * (b - 1001);
       a >>= 20;
       a *= 10;
       a += 100;
       a %= uint256(1).sqrt() ^ 91;
       a *= (a +
