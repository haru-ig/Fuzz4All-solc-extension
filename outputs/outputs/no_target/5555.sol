pragma solidity ^0.8.0;
contract O {
    struct N {
        uint x;
        function f(uint j) {
        uint x;
            x = 2;
            uint x;
            (x,x) = (x-3,(x,x));
        }
    }
    function f(uint j) {
        N self;
        self.f(j);
    }
    function f() public returns (uint) {
        return 3;
    }
}
