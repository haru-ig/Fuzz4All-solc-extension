pragma solidity ^0.8.0;
contract O {
    uint public x;
    address payable public y;
    event xchange(uint y);
    function y(uint x) external returns (uint) {
        return ((x == 0) * x / 2) + (x == 0) + x;
    }
}
