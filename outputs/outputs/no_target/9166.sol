pragma solidity ^0.8.0;
contract mycontract {
    constructor(uint a_global_uint) public {
        uint8 x;
        (x, x) = simpler_abi3.x();
        (x, x) = simpler_abi3.x();
    }
}
```

# 7. Reference

[A Simple example of the "abi"-specification, including the specification of the standard library.](https:
