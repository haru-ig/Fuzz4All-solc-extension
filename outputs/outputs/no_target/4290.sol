pragma solidity ^0.8.0;
contract O {
  function F() public pure returns (uint p) {
  }
}
`
let contract = getContract('contracts/3.sol', '')


})

describe('testing', () => {

  it('testing', assert => {
    assert(true)
  })
})
