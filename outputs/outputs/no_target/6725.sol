pragma solidity ^0.8.0;
interface Test
{
    function add(uint, uint) external pure returns (uint result);
    function mul(uint, uint) external pure returns (uint result);
    function add2(uint,uint)external pure returns (uint result);
}
