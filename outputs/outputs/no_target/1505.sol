pragma solidity ^0.8.0;
contract Semantic0034 {
    uint x;
    uint y;
    uint z;
    function assign() public payable {
        z /= x * y;
    }
}

pragma solidity ^0.8.0;
contract Semantic0034 {
    uint q;
    uint r;
    uint z;
    uint p;
      uint c;
      uint x;
    function assign1() public payable {
        z /= x * p * q / (q + r * c);
    }
}
