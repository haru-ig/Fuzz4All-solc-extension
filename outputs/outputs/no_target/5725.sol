pragma solidity ^0.8.0;
contract Zam {
    uint x;
    uint z;
    uint16 w;
    uint8 f;
    uint8 s;
    bytes32 KY;
    enum KA {
        YY, YE, YY, YX, YZ
    }

    bool b = true;
    address c = address(0xdeadfacedeadfacedeadfacedeadfacedeadfacede);
    bytes4 d = bytes4(keccak256('d()'));
    function a() public view returns (uint e, ZA h, uint16 i, string memory j, bytes32 k, bool l) {
        bytes memory m;
        string memory n;
        bool o;
        uint16 p;
        uint8 m1;
        uint8 m2;
        bytes32 m4;
        uint128 n1;
        uint8 n4;
        bytes32 q;
        uint8 t1;
        bytes memory v;
        uint16 w;
        ZA x;
        assembly {
            a := calldataload(0xc)
        }
        c = contract(keccak256(c));
        v = new bytes(1);
        v[0] = 0xff;
        o = calldata(0x10);
        if (x == 1) {
            revert(0, 0);
        }
        y = 0xdeadfacedeadfacedeadfacedeadfacedeadface;
        if (o) {
            v[0] = 0;
        }
        x = ZA(keccak256(keccak256(o? n : v)));
        if (x.test() < 0) {
            v = new bytes(1);
            v[0] = 0xff;
            revert(0);
        }
        if (m.length < 0 && v.length > 0) {
            revert(-1);
        }
        n4 = 1;
        if (n > n4) {
            revert(-2);
        }
        if (n == n4 || n > 0) {
            revert(uint16(n));
        }
        if (n!= null) {
            m.length = 10;
        }
        else if (n > n4) {
            revert(-1);
        }
        m.length -= 1;
        m += n;
        m1 = m[10];
        m2 = m.length;
        m.length -= 1;
        if (m.length == 1 && m1 > m2 - 1 - n.length) {
            revert(uint8(n.length));
        }
        if (m < n) {
            revert(-1);
        }
        if (0x10 <= m2 - m) {
            revert(-1);
        }
        if (35185 <= m.length) {
            revert(uint16(m.length));
        }
        if (o >= m1 && o < m2 - 1 - n4.length) {
            revert(
