pragma solidity ^0.8.0;
contract SimpleExample22 {
    constructor() public {
        uint256 a;
        uint256 b;
        uint256 c;
        uint160 d1;
        uint8 d2;
        uint160 d3;
        uint256 e;
        uint256 f;
        uint256 g;
        uint256 h;
        uint256 i;
        uint256 j;
        uint256 k;
        (a > b)? ( c > d? ( a > e * b * c * d * e * f ) : ( a + b >= num ) ) : ( c > d? ( b + c >= num ) : ( a > b - 1 ) );
        a + b - 1 = a == (1 > b)? num : 1 > 2? 2 : 2;
        ( a + b * 3 / num) * c > (b * 3 * num) / c - (a * 20 + b * 5) % 10 + ((a + b) * 1 < 5) + (2 < 1) + (a * b >= 1);
        uint256 l = 1 > (a + b < a + b + 2)? a + ((1 > (1 < 1))? b : a) : 2;
        c = (a == b + 32)? a + ((1 < 1))? b : a : 1;
        ( b + 2 + 3 + 1 * (c + 8) + 4 % 3 / 2 % 3 % 7)  * (4 - 5 + 6 + num - 3) >= 57 + (18 + 7) / 0x10000 * 80002 * 0x2000 | 1 % 80002 * 0x2 * 0x1002 + (8 < 0) - 2 < (a + c) * (a | b | c + 4));
        uint256 m = 1 > (((num + 1) <= 3 + 3) | ((4 < num) * ((b < d) |= num >> 1 % 3 == 1)) | (1 > num) < (3 > (2 < (a + a) %
