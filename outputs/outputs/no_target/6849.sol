pragma solidity ^0.8.0;
contract TestMutated731 {
    function mul(uint x, uint y) public returns (uint) {
        uint z0 = 7;
        uint z;
        uint z1;
        uint z2;
        uint z3;
        uint z4;
        if (z1 > 0) {
            if (z0 >= 0) {
                z2 = mul(z1, y);
            } else {
                z2 = mul(z1, x);
            }
        } else {
            if (z0 < 0) {
                z3 = _neg_0(z3 + z0);
            }
            if (z0 < 0 - x) {
                z2 = mul(z1, x - y);
            }
        }
        if (z2 < 0) {
            z4 = 0 - z2;
            uint z5 = _neg_0(0 - z);
        }
        uint z6;
        if (z4 < z3) {
            z5 = mul(z4, x);
        }
        return z6;
    }

    function _neg_0(uint x) private pure returns (uint) {
        uint z;
        if (x < 0) {
            z = x - 1;
        }
        return z;
    }
}

pragma solidity ^0.8.0;
contract TestMutated732 {
    uint internal field0;
    uint x;
    uint private y;
    uint64 z0;
    uint256 z;
    uint32 private z1;
    uint64 private z2;
    uint256 z3;
    uint32 z4;
    uint64 z5;
    uint256 z6;

    constructor() public {
        x = 0; x;
        y = 7; y;
    }

    function test() public returns(uint, uint) {
        uint32 r; r;
        uint64 rr; rr;
        (, z1, z2) = _neg_zero_1(_neg_zero_2(0));
        (r, z0) = _neg_zero_3(0);
        (r, z) =
