pragma solidity ^0.8.0;
contract Mutate45_ {
    uint public a;
    uint public b;
    uint public x;
    constructor(uint256 y, uint256 x) public {
        a = (x + 2) * 2 + x + 3;
        b = x + 1;
    }
}
