pragma solidity ^0.8.0;
 contract B {
    mapping(uint => uint) a;
    constructor() public {}
    uint f() public {
        a[1] = x;
    }
     function x() public view returns (uint) {
       return a[1];
     }
}
