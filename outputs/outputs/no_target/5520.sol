pragma solidity ^0.8.0;
contract O {
	mapping(uint=>uint) public y;
	function f() public returns (bool) {
		uint x = 1;
		uint z = 0;

		if (x > x-z) {
    		return true;
		} else {
			return false;
		}
    }
}
