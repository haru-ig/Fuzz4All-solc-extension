pragma solidity ^0.8.0;
contract modifierWithParam {
    function f(uint x) public view returns (uint) {
        if (x == 0) {
            return 0;
        }
        uint temp = (x - 1) + 2;
        x = x +  i;
    }
}
