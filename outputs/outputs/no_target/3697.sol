pragma solidity ^0.8.0;
contract Test {
    function main() public {
        require(false);

        {
        require(A.f() < 2);
        emit E ();
        }
    }
    uint f() public view returns (uint){
        return 2;
    }
}
