pragma solidity ^0.8.0;
contract TestMutated729 {
    function mul(uint x, uint y) public pure returns (uint) {
        uint z;
        uint s;
        uint h0 = 0;
        uint h1;
        uint h2;
        uint h3;
        uint h4;
        bool isMinus = (y < 0);
        if (isMinus) {
            s = 1;
            y = -y;
        } else {
            s = -1;
        }
        if (x < z0) {
            bighz(x, x, x);
            h1 = x;
            h4 = div(z101, x);
            h3 = mul(div(x080, x), h4);
            s = s * isMinus;
            h2 = x000;
            h0 = div(h0, x);
        } else {
            h3 = 0;
            h1 = x;
            h0 = div(h0, y);
            h4 = mul(div(x080, y), h0);
            isMinus = isMinus * (x < 0);
            h2 = x000;
        }
        uint z1 = 0;
        uint z2;
        uint h5;
        uint h6;
        uint z5;
        uint z6;
        z1 = add(h1, h3);
        if (h2 >= 0) {
            z2 = add(h2, h4);
            z6 = 0;
            z5 = 0;
            h6 = div(z080, z1);
        } else {
            z2 = sub(h2, h3);
            isMinus = isMinus * z;
            z1 = sub(h1, h4);
            h1 = h1 + z;
            h6 = div(x080, h1);
        }
        uint z3 = 0;
        bool isMinus1 = (h1 < z);
        bool isMinus2 = (h2 < z);
        while (isMinus2!= isMinus1) {
            z3 = add(z3, div(h5, h1));
            if (!isMinus1) {
                z5 = h1;
                h1 = sub(h1, h3);
            } else {
                z5 = 0;
