pragma solidity ^0.8.0;
pragma solidity >=0.8.0;
contract third {
    function g() public pure(a,b), pure{
        address a = address(1);
    }
    function h() public pure(a,b), pure{
        address a = address(0);
    }
    function pure() {
        address a = address(1);
    }
}
@v
