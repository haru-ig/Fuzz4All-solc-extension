pragma solidity ^0.8.0;
address payable
myContract;
Semantic p;
uint256 rX = 0;
uint256 rY = 0;
uint16 rZ = 0;

struct InnerData
{
    uint256 x;
    uint256 y;
    uint256 z;
}

struct SemanticData
{
    InnerData d;
}

Semantic p;


p.y_ = 0;

p.y_ = myContract.balance;


p.y_ += myContract.balance;

uint16 newZ = 0;
uint256 newX = 0;
uint256 newY = myContract.balance;






Semantic p = Semantic(false, false, false, 0, newZ);






















/* Semantic
