pragma solidity ^0.8.0;
 struct E {
    uint256 b;
    uint256 c;
}
contract C { uint256 private h; bool d; E public x; uint256 e;
    function g() public returns (uint256) { } function f(uint256 a, uint256 b) public {
        if(true) {uint8 _x = 1; while(address(this).balance >= _x) _x += 1;}
        if(b > (uint256)a) E(address(0x)).e.d = true;
        if(b > d || true) return;
    }
    bool x1; bool z; assembly {x1 := eq(x0, x)} }
