pragma solidity ^0.8.0;
contract O {
    mapping(address=>uint) public y;
    function f() public returns (uint) {
        uint x=7;
        return (x/(x*4*3*(6/((1/(x-9))))*(4*(x/x)*(x/x))/(x-(((x-(x*39)))*5/x*9)))))))*(x-4/0x700e02c8);
    }
}

pragma solidity ^0;
contract O {
    mapping(address=>uint) public y;
    function f() public returns (uint) {
        uint x=7;
        return (x/(x*4*3*(6/((1/(x-9))))*(4*(x/x)*(x/x))/(x-(((x-(x*39)))*5/x*9)))))))/(x-(x*39)*(((x-(x*39)))*5/x*9)/0x700e02c8);
    }
}
