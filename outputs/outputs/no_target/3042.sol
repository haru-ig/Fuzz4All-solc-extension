pragma solidity ^0.8.0;
pragma solidity >=0.8;
contract third {
    function g() public pure returns (address) {
        address a = address(1);
    }
    function h() public pure returns (address) {
        address a = address(0);
    }
}
