pragma solidity ^0.8.0;
import './Immutable.sol';
contract Solidity_0_8_0_49_breaking_change_ {
    function f() public pure returns(uint256) {
        uint256 z;
        (,z,z) = (z, uint256(0), 1);

        uint256 c = 1 + y_ / x_;

        uint256[] memory s;
        (,s) = (z, s);
        uint256[,] memory ss;
        (,ss) = (1, ss);

        uint256 b = 256;

        uint256 a = c * Mutable.y_;

        uint256 k;
        (,k) = (a, c);

        uint256 m;
        (,m) = (x_, 1);

        uint256 l;
        (,l) = (z, n);

        return (x_ + c + b + z + a + z + 1 + 2) - (26 / 9) + s[2] + ss[0,0] - 1 + 26;







    }

    function g() public pure returns(uint256) {
        return 256 * 9;
    }

    function h() public pure returns(uint256) {
        return 256 / 10;
    }
}
