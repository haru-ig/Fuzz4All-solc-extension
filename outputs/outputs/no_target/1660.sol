pragma solidity ^0.8.0;
import '../../generated/Semantic0015a.sol';
contract Semantic0015b {
    constructor (uint256) {}
    function f(uint256 input) public {
        Semantic0015b.f(input);
    }
}
