pragma solidity ^0.8.0;
contract modulator5 {
    uint256 constant public D = 3;

}
contract EtherscanExample {
    uint256 public d;
    constructor() { d = modulator4.D; }

    function test_simple_logic_contract() public {
        uint256 num = 10;
        uint256 result = 42 + 42;
        assert(result == 10);
        result = 10 / 3;
        assert(result == 3);
        result = (-1) / (2 * M);
        assert(result == (D ^ 25) % D);

        modulator5 mc;

        mc.d += 11;
        mc.d = D;
        result = num / mc.d;
        assert(result == N);
    }
}
