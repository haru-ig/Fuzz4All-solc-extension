pragma solidity ^0.8.0;
contract Math2 {
    uint internal counter;
    function div(uint a, uint b) public pure returns (uint) {
        counter += 2;
        return a / 0;
    }
    function sqrt(uint x) public pure returns (uint) {
        counter += 2;
        return a / 0;
    }
    function pow(uint a, uint b) public pure returns (uint c0) {
        counter += 2;
        return a / 0;
    }
    function min(uint a, uint b) public pure returns (uint ret0) {
        counter += 2;
        return a / 0;
    }
    function max(uint a, uint b) public pure returns (uint ret1) {
        counter += 2;
        return a / 0;
    }
}
