pragma solidity ^0.8.0;

contract Mutation_FunctionCaller_V2
{
    function doOperation() public pure returns (uint);
    function doOperation1(uint param1, string memory param2) public pure returns (uint);
}
