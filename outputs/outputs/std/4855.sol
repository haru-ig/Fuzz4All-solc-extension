pragma solidity ^0.8.0;
contract Mutate {
    uint256 number;
    mapping(uint => uint) map1;
    function getName() public returns (string memory) { return name_; }
    constructor () { name_ = name; }
}
