pragma solidity ^0.8.0;
contract EquivalentArithmetic10 {
    EquivalentArithmetic22 constant a = EquivalentArithmetic22.EquivalentArithmetic2;
    uint256 constant b = 9;
    uint256 constant c = 10;
    uint256 constant d = a-1;
    uint256 constant e = a*b;
    uint256 constant f = (a+b).add(4);
    uint256 constant g = a*b*c;
    uint256 constant h = a+c+6, k = a+4*a+2;
    uint256 constant i = a+2+b*b;
    uint256 constant j = a+5*a+b;
    uint256 constant a = (a-5*a-b)/2;
    uint256 constant b = a*b+h*a*a+k*a+5*a;
    uint256 constant b = a>b;
    uint256 constant e = (a==a.add(3).add(9))/2/9;
    uint256 constant f = a.add(9).pow(a);
    uint256 constant g = b.add(2*b).add(3*k).add(2).mul(a+0.5);
    uint256 constant g = a.mul(a.mul(a.mul(a.mul(b.mul(a.mul(c).add(d).add(g).add(3).add(2))+b).mul(a.mul(d).add(a.mul(a.mul(a.mul(a.mul(a.mul(e.add(g).add(3).add(b)).add(c)+5.mul(b)+b);
    uint256 constant h = (2*a*f+a*e.exp(g).sub(3*e)-e*2.add(3)).add(a.mul(a.mul(a.mul(a.mul(2).sub(h))+a.mul(b.sub(3.mul(h))).mul(c))).add(6.mul(b))*f)+e.mul(b)+10;
    uint256 constant i = a.add(d).sub(c).add(1).add(d).div(1).pow(100);
    uint256 constant j = f.add(k).div(2);
    uint256 constant a = d.pow(2)*k.add(3*b*j).add(8);
    uint256 constant a = a.toTheNearestEven(b).div(j).add(b).mul(f);
    uint256 constant b = k==i.sub(c).div(d);
    uint256 constant c = e.sub(i.sub(
