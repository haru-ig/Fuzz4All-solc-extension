pragma solidity ^0.8.0;
contract New
{
    New x;
    uint256 x1;
    uint256 x2;
    uint256 x3;
    uint256 x4;
    uint256 x5;
    uint256 x6;
    uint256 x7;
    uint256 x8;
    address y;
    address x1_addr
    address x2_addr
    address x3_addr
    address x5_addr
    address x6_addr
    address x7_addr
    uint256 x8_addr
    uint256 y_addr
    event Test (uint256 indexed index, address indexed caller, uint256 indexed x_addr, string indexed x_bytes, address indexed y_addr, string indexed y_bytes);
}
