pragma solidity ^0.8.0;
contract TestArray{
    function setOne(uint256 value)public returns(bool){
        address(this).balance;
        address(this).balance;
        arrayOfIntegers.push(value);
    }
}
