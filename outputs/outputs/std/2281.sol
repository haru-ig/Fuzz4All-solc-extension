pragma solidity ^0.8.0;
interface ICanSave {
    function save(uint x) external returns (uint);
}

pragma solidity ^0.8.0;
interface ICanSave {
    function save2(uint[] memory xs) external returns (uint32);
}
