pragma solidity ^0.8.0;
contract A{
address internal a;   function f() public {
        address b = c();
        address d = new E();
        E fuzz = a0();
        a0.transfer(b, a.call{sig:"()call"}()
        a(
            "foo"
        );

            E(a).foo();
            c(E(a).f()
    )

        fuzz(c(d.a()));
    }
    function E(address a) public {
        address b;
        b = a;
        b.transfer(b, E(a.call{sig:"()call"}()));
    }
    function c(uint256) public pure {
        require(false);
    }
    struct E {
        constructor (address _a) {
            call(this);
        }
        function foo() public pure {
            require(false);
        }
    }
}
