pragma solidity ^0.8.0;
contract MyContract {
  constructor() {}
  function totalSupply() public pure returns (uint32){
  }
  function balanceOf(address addr) public view returns (uint32){
  }
  function transfer(address _to, uint32 _value) public returns (bool success){
  }
  function transferFrom(address _from, address _to, uint32 _value) public returns (bool success){
  }
}
