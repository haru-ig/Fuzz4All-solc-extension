pragma solidity ^0.8.0;
contract Mutate14 {
    uint a;
    uint b;
    uint c;
    uint d;
    constructor() {
        c = b + (uint(uint(uint(uint(uint(a))) * uint(b))) * (uint(d) * uint(c)));
    }
}
