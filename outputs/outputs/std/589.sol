pragma solidity ^0.8.0;
contract HelloWorld {
  uint256 constant TWO = 0x0080000000000005;
  function mod(uint256 x) public pure returns(uint256) {
    if(x >= TWO) return uint256(X) - TWO*(x/TOU_ONE_BITS)-X;
    else return x;
  }
}
