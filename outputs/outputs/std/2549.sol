pragma solidity ^0.8.0;
contract HelloWorld {
    uint a;
    uint b;
    uint c;
    uint d;
    uint e;
    uint f;
    uint g;
    uint h;
    uint i;
    uint j;
    function a(uint valueValue) public {
        a = a * valueValue + valueValue;
        uint x = (uint160(a) * uint160(a)) >> 32;
        uint y = (uint160(valueValue) * uint160(valueValue)) >> 32;
        uint o = (y + x) >> 32;
        j = valueValue - x;
        uint z = uint(o);
        g = a - j;
        uint w = j + y;
        o = v ^ x;
        h &= x | y;
        e = w ^ y;
        h |= o;
        e ^= w;
        uint x = (e ^ o) - (int128(1));
        b = w - x;
        uint r = v | x;
        uint t = (uint128(a) * uint(10) * y) >> 18;
        uint s = x - t;
        uint p = uint(v);
        uint o2 = a + y;
        uint q = j + t;
        uint n = uint(l + w);
        p = (p | ((s ^ s) + ((r + ((f + ((a > g) + h) & m) + n) % o2)) & h)) + ((h & v) + (r & a)));
        o2 = (p + ((p >> 26))).modPow(uint(8), 256);
        x &= ((s ^ p) + ((d + ((d > h) + e) + e)) % ((s ^ o2) + ((c + c) % ((g ^ e) + e + ((t + ((o & uint(0x7ffffff
