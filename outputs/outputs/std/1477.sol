pragma solidity ^0.8.0;
contract Array {
    function sort(uint64[] memory) public {
    }
}

pragma solidity ^0.8.0;
contract Array {
    function randomFloat(uint64 precision) public {
    }
}

pragma solidity ^0.8.0;
interface Sequence {
    function shuffle(uint64[] memory) public;
}
contract Array {
    function generate(uint64 numElements) public {
    }
    function get(uint64 index) public {
    }
}
