pragma solidity ^0.8.0;
contract New
{
    mapping (uint256 => mapping (uint256 => bool)) public arrayMapping;
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    uint256 public z1;
    event Test (uint256 indexed index, address indexed caller, uint256 indexed balance);
}
pragma solidity ^0.8.0;
contract New
{
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    uint256 public z1;
    event Test (uint256 indexed index, address indexed caller, uint256 indexed balance);
}
pragma solidity ^0.8.0;
contract New
{
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    uint256 public z1;
    mapping (uint256 => mapping (uint256 => bool)) public arrayMapping;
    event Test (uint256 indexed index, address indexed caller, uint256 indexed balance);
}
pragma solidity ^0.8.0;
contract New
{
    mapping (uint256 => mapping (uint256 => bool)) public arrayMapping;
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    mapping (uint256 => bool) public arrayBool;
    uint256 public z1;
    event Test (uint256 indexed index, address indexed caller, uint256 indexed balance);
}
pragma solidity ^0.8.0;
contract New
{
    mapping (uint256 => mapping (uint256 => bool)) public arrayMapping;
    mapping (uint256 => bool) public arrayBool;
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    mapping (uint256 => mapping (uint256 => bool)) public array2Mapping;
    uint256 public z1;
    event Test (uint256 indexed index, address indexed caller, uint256 indexed balance);
}
pragma solidity ^0.8.0;
contract New
{
    mapping (uint256 => mapping (uint256 => bool)) public arrayMapping;
    mapping (uint256 => bool) public arrayBool;
    mapping (uint256 => bool) public array2Bool;
    uint256 public x;
    bool public x1;
    address public y;
       uint256 public z;
    mapping (uint256 => mapping (uint256 => bool)) public array3Mapping;
    mapping (uint256 => bool) public array3Bool;
    uint256 public z1;
    event Test (uint256 indexed index, address indexed caller,
