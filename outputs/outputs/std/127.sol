pragma solidity ^0.8.0;
address _contract;
function _max_int(uint x, uint u) public pure returns (uint) {
        if (x == u) { return x; }
        if (x < u) { return x; }
        return u;
}

pragma solidity ^0.8.0;
contract _contract is MutateProgram {
    function _max_int(uint x, uint u) public pure returns (uint) {
        if (x == u) { return x; }
        if (x < u) { return x; }
        return u;
    }
}

pragma solidity ^0.8.0;
contract _contract is MutateProgram {
    function _max_int(uint x, uint u) public pure returns (uint) {
        if (x == u) { return x; }
        if (x < u) { return x; }
        return u;
    }
}

pragma solidity ^0.8.0;
contract _contract is MutateProgram {
    function _max_int(uint x, uint u) public pure returns (uint) {
        if (x == u) { return x; }
        if (x < u) { return x; }
        return u;
    }
}

pragma solidity ^0.8.0;
contract _contract is MutateProgram {
    function _max_int(uint x, uint u) public pure returns (uint) {
        if (x == u) { return x; }
        if (x < u) { return x; }
        return u;
    }
}
