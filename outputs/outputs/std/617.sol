pragma solidity ^0.8.0;
contract ArrayTest {
  uint8 [3] private y;
  function test() public pure {
    const z = uint8(1);
    y[0] = X;
    (bytes memory p, uint8 x) = bytes(mod(z));
    mod(2);
    uint8 x1 = x + 1;
  }
}
