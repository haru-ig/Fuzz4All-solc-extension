pragma solidity ^0.8.0;
contract Cast_equivalent {
    uint256 public num;
    Constructor_i(_num) public {
        num = uint264(_num);
    }
    function Constructor_i(uint _num_i) public {
        num = uint256(_num_i);
    }
}
