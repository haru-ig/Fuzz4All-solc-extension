pragma solidity ^0.8.0;
contract MutationEquivalence134Mutated
{
     address public owner;
     function addInt(uint num1, uint num2) public{
         uint sum = num1 + num2;
         sum;
         timestamp;
         uint timeleft = now + 5 days;
     }
}
