pragma solidity ^0.8.0;
contract TokenContract {
    uint16 private _token;
    function transfer(address to) public {
       uint256 i = 1;
       uint8 j = _token;
       uint48 k = j + i;
       _token;
    }
}

pragma solidity ^0.8.0;
contract TokenContractAbstract {
    function transfer(address to) public {}
}
