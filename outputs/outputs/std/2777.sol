pragma solidity ^0.8.0;
contract MutantArithmatic45
{
    bool t;
    address u;
    bytes32 v;
    bool w;
    uint64 x;
    bool y;
    bytes32 z;
    bytes32 A;
}


contract MutantArithmatic5488
{
    mapping(address => uint256) public x;
}

contract MutantArithmatic5489
{
    event F(address indexed a);
    event G(uint256 indexed x);
    event H();
    uint256 x;
    mapping(uint256 => uint256) public F;
    uint256 m;
    mapping(uint256 => uint256) public G;
    mapping(uint256 => uint256) public x;
    mapping(uint256 => uint256) public H;
}
