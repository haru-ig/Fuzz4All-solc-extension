pragma solidity ^0.8.0;
contract test {
    function testMap() public returns (mapping(uint => uint)) {
    }
    function testMap() private returns (mapping(uint => uint)) {
    }
    function testMapKey() public returns (uint) {
    }
    function testMapKey() private returns (uint) {
    }
}
