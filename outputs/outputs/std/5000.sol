pragma solidity ^0.8.0;

contract Array {
    function subarray(uint a, bytes memory b) internal pure returns (uint128) {

    }
    function max(uint256 a, uintb b) internal pure returns (uint) {
        return a.max(b);
    }
    function min(uint256 a, uintb b) internal pure returns (uint) {
        return a.min(b);
    }
    function summ(uint256 a) internal pure returns (uint) {
        return a;
    }
    function set(address a, uintb b, bytes memory c) internal pure {

    }
}
