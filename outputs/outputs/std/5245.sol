pragma solidity ^0.8.0;
contract MyContract {
    uint myuint;
    uint constant myconst = 3 * 6 / 9;
    function f(uint n) public {
        if (n < myuint) {
            uint x = 0;
        } else {
            uint x = 0;
            uint y = 3 * myconst - myuint;
            uint z = myconst + 5 * myuint + myuint / 2;
        }
        uint x;
        uint y;
        uint z;
        uint w;
        w = myconst + 60 / 7378;
        uint x = 0 - 3 - myuint / 7379 *
            myconst *  97 * 6 - 4 *
            myuint;

    }
}
