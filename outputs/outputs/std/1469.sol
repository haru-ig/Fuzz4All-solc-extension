pragma solidity ^0.8.0;
contract CriticismTest is Criticism {
    function getCritics(uint64[] memory) public {
        emit PriceChangeEvent(a[3]);
        emit CommentChanged(criticComment);
        emit ScoreChanged(a[2]);
    }
}
