pragma solidity ^0.8.0;
contract Array {
    function _length(address[] memory from) public returns (uint) {
    }
    function _min(address[] memory from) public returns (uint) {
    }
    function _max(address[] memory from) public returns (uint) {
    }
    function _sum(address[] memory from) public returns (uint) {
    }
    function _sort(address[] memory from) public returns (address[]) {
    }
}
