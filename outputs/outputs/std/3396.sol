pragma solidity ^0.8.0;
interface Cond_MutatorMutatorMutatorMutatorMutator {
  function check(address _cond) external;
  function replace(uint _val, address _cond) external;
}
interface Check {
  function check(address _addr) external returns (uint);
}
