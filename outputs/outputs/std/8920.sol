pragma solidity ^0.8.0;
contract ExampleStruct10 {
    uint x;
    uint y;
    bool z;
    uint f(uint) returns (uint);
    constructor(uint a_x, uint a_y) {
       x = a_x;
       y = a_y;
        uint x = a_x;
    }
}
