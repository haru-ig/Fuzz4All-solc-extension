pragma solidity ^0.8.0;
contract ArrayContract {
    function findmin(uint[5] memory) external returns(uint) {
        uint x;
        x;
    }
    function findmax(uint[5] memory) external returns(uint) {
        uint x;
        x;
    }
}
