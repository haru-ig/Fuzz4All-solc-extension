pragma solidity ^0.8.0;
contract B1 {
    function f1() public view returns(bytes memory) {
        return bytes(0);
    }
    function f12(bytes memory b1, uint256 b2) public view returns(bytes memory) {
        f7 = 16;
        return bytes(0);
    }
}
