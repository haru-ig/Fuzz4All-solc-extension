pragma solidity ^0.8.0;
contract Storage {
    function write(uint a, uint x) public {
        a--;
    }
    function read(uint a) public view returns (uint)
    {
        x=x.sub(10);
        uint b;
        b = b.add((a*(((b*2183)*117))+70143587322));
        b*=60400;
        b*=-3930924645;
        return (uint)b.div((b*10)+x);
    }
}
