pragma solidity ^0.8.0;
contract Array {

    function minIndex(uint64[] memory) public {
    }

    function maxIndex(uint64[] memory) public {
    }

    function sum(uint64[] memory) public {
    }

    function set(uint64 index, uint64 element) public {
    }

    function get(uint64 index) public {
    }

    function sort(uint64[] memory) public {
    }
}
