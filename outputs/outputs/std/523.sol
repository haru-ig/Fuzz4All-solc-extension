pragma solidity ^0.8.0;
contract test {
    function testArray(uint32 i) public returns (uint32) {
        return testArray;
    }
    function testArray(uint32 i, uint32 j) public returns (uint32) {
        testArray += testArray + i;
        return testArray;
    }
}
