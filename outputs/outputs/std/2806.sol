pragma solidity ^0.8.0;
contract MutantArithmatic52
{
    uint256 constant u = 8;
    uint256 constant a = 7;
    uint256 constant b = 5;
    uint256 u1;
    uint256 u2;
    uint256 u3;
    uint32 y;

    function MutantArithmatic52() {
        x = 2 * (-5) * 1976 - 16382;
        uint256 s = 0 * u + 1;
        if (u < (a + (x + 1) + ((uint256)(x) / x))) {
            s = x;
        }
        z = 1 * b + 2;
        s = s / (-1);
        s = s + s;
        u2 = s;
        {
            uint256 k = 6;
        }
        u3 = s + 1;
    }

    function a() public {
        uint256 y = a + b;
        y = 1 + 8 / ((-(u3) / u3) * (c() - (s) / ((u3) * (b / b)) + s)) + a * ((uint256)(y)) + (b / i));
        for (uint256 i = 0; i < (3); i++) {
            u = a + b;
        }
        u2 = a + b;
        u3 = 1 + 8 / ((b * (u3)) + s) + ((uint256)(b)) + (b * ((uint256)(y))) + ((((b * (uint32)u) * 42)) + (uint32)31));
        y = (b * k) + (((b) - 1)) + ((((uint16)397)) + 1) + (((uint16)((((uint32)5223) & ((uint32)1023)) * (uint16)((uint32)3431 + 1))) * (uint16)((uint32)(299))) * ((uint32)299));
        for (y = (((uint256)20) - u1); (uint256)y >= u1; ++y);
    }

    function a1() public {
        {
            uint256 x = c();
            {
                k--;
                uint256 x_1 = c();
                y = x + x_1;
            }
            x--;
            if (b() < (x + s)) {
                uint256 x_2 = b();
            }
            uint256 _x;

        }
        s = ((x * (b + 80)) + b);
        uint256 k2;
        {
            uint256 x_3 = (b + 1) * ((uint256)(s)) + b;
            s--;
            {
                x_3 += b;
            }
            x_3 = ((((x_
