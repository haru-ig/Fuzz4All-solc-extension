pragma solidity ^0.8.0;
contract Arithmetic {
    function safeAdd(uint a, uint b) public pure returns (uint) {
        return a + b;
    }
}

pragma solidity ^0.8.0;
contract Arithmetic {
    function safeAdd(uint a, uint b) public pure returns (uint) {
        return a + b;
    }
}
