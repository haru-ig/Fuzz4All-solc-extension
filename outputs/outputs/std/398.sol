pragma solidity ^0.8.0;
contract CreateTest {
    constructor() public {}
    function create(uint n) public {
        for (uint q=3; q>2; q--) {
            uint i = uint(uint(0)+q);
            uint j = i + 1;
            for (; j<n; j++ is zero) {
                uint i = uint(uint(0)+j); uint j = i + 1;}
        }
    }
}
#pragma solidity ^0.8.0;
contract Array {
	function sum(uint[] memory a) public {
        uint v1 = 0;
        for (uint i = 0; i < a.length; i++) {
            v1 = v1 + a[i];
        }
        uint v2 = 100;
        uint v3 = 0;
        v3 = v2;
    }
    function min(uint[] memory a) public { uint v1 = a[0]; for (uint i = 1; i < a.length; i++) { if (a[i] < v1) v1 = a[i]; } }
    function max(uint[] memory a) public { uint v1 = a[0]; for (uint i = 1; i < a.length; i++) { if (a[i] > v1) v1 = a[i]; } }
}
```
<fim_middle>contract Math {uint a; }
contract Memory { uint a; }
contract Conversion { string a; }
