pragma solidity ^0.8.0;
contract MyContract {
    uint constant myconst = 3 * 6 / 9;
    uint myuint;
    function g(uint n) public {
        myuint = 4 * 48 / 3;
        if (n < myuint) {
            bool x;
        } else {
            uint x;
        }
        uint x;
        uint y;
        uint x = myuint - 2 * myconst;
        uint y = myconst + 3 * myuint;
    }
}
