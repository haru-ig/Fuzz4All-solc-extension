pragma solidity ^0.8.0;
contract Subroutine {



    function test(uint x, uint y) public pure returns (uint256) {
        uint sum = 0;
    }
}

contract ArrayWithSubcontract {
    uint[] public numbers;

    function add(uint number, uint other) public pure returns (uint) {
        return number + other;
    }
}
