pragma solidity ^0.8.0;
contract MyContract {
    uint public myuint;
    function f(uint n) public {
        if (n < myuint + 1) {
            uint x;
        } else {
            uint x;
        }
        uint x;
        uint y = (myuint + 1) - 2 * myuint + 1;
    }
}
