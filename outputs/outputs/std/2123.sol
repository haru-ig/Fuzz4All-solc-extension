pragma solidity ^0.8.0;
contract Mutated_5_6_8 {
    constructor() {

        address adr = 0;
        adr.transfer(address(mut.max));
        adr.transfer(0);
        address[] memory maddr = new address[](1);
        maddr[0] = 0;
        maddr[0].transfer(0);
    }
}
