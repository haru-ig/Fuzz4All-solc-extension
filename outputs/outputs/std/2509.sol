pragma solidity ^0.8.0;
contract Array {
    uint public length;
    uint[] public array;
    function push(uint x) public returns (uint y) {
        _token;
        uint256 length_before = length;
        length;
        x;
        length_before;
    }
    function pop() public returns (uint x) {
        _token;
        uint256 length_before = length;
        length;
        length_before;
    }
    function getMin() public returns (uint x) {
        _token;
        uint256 length_before = length;
        length;
        length_before;
    }
    function getMax() public returns (uint x) {
        _token;
        uint256 length_before = length;
        length;
        length_before;
    }
    function getSum() public returns (uint x) {
        _token;
        uint256 length_before = length;
        length;
        length_before;
    }
    function sortAscending() public returns (uint x) {
        _token;
        uint256 len_before = length;
        length;
        len_before;
    }
    function sortDescending() public returns (uint x) {
        _token;
        uint256 len_before = length;
        length;
        len_before;
    }
}
