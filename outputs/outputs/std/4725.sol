pragma solidity ^0.8.0;
contract Arrays {
    uint a;
    uint[20] m;
    uint[5] s;

    function f1() external {
        a;
        uint[10] b;
        uint64 d;

        a;
        uint d;

        a;
        uint [] e;

        a;
        uint j = 3;
        uint[3] l;
    }

    uint[20][] r;
    function f2() external {
        a;
        m;
        s;

        a;
        uint[5] [] [];

        a;
        uint[5][3] i;

        a;
        uint j;
        uint[3] l;
    }

    function f3() external {
        uint x;
        uint[] y;
        uint j;

        uint x;
        uint[] [20] y;
        u[3] x;

        uint x;
        uint[3][20] y;
        uint[20] [] x;

        uint x;
        uint[20][3] y;
        uint[3] [] x;

        uint x;
        uint[10000] y;
        bytes32 [] x;
    }

    function f4() external {
        uint j = uint8(uint8(uint32(uint32(uint128(uint128(19046) / 80983) * uint8(uint0) / (uint0 + uint9))) - uint129(uint64(-uint32(-uint64(uint64(338517251)) / 1956106116) * -uint8(uint0) * uint125) / (uint0 + uint191))) + uint64(uint32(-182060369)) / (uint7)))) / uint24(uint32(uint32(uint25)) / (uint32(uint0) + uint8(uint32(uint8(uint64(uint32(uint128(uint128(12118)) / 23771) * uint8(uint0) / (uint0 + uint249) * uint8(uint0)) + uint32(uint8(uint258(uint64(2538))) / 81461)) * uint8(uint0) / (uint0 + uint81) + uint128(uint20548)) * uint8(uint0) / (uint0 + uint854) + uint32(uint8(uint23413))) * uint8(uint32(uint32(uint32(uint64(224
