pragma solidity ^0.8.0;
contract MyContract {
    uint myuint;
    uint constant myconst = 3 * 6 / 9;
    function f(uint n) public {
        if (n < myuint) {
            uint x;
        } else {
            uint x;
        }
        uint x;
        uint y;
        x = myuint + 2 * myconst;
        y = myconst - 3 * myuint;
    }
}
