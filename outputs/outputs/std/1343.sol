pragma solidity ^0.8.0;

pragma solidity ^0.8.0;
contract ArraysMutated {
    bytes32 private immutable symbol_ = "ARRAY_MUT";
    bytes32 private immutable name_ = "NAME2";
    bytes32 private immutable decimals_ = "ARRAY_DECIMALS2";

    uint public constant N = 31;

    uint[] private testArray;

    uint public value2_ = 0;
}
