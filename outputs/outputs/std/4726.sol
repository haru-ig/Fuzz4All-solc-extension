pragma solidity ^0.8.0;
contract C {
  uint256 public c;
  int intArr1;
  mapping(uint => uint256) g;
  struct M {
    uint256 a;
  }
}
contract D {
  uint[] public f;
  uint[1] public g;
  uint[3] public h;
}
contract E {
  mapping(uint => uint256) g;
  uint256 public i;
  uint[1] public j;
}

pragma solidity ^0.8.0;
contract C {
  uint256 public c;
  constructor (uint256 _c) C contract.address);
  int intArr1;
  mapping(uint => uint256) g;
  struct M {
    uint256 a;
  }
}
