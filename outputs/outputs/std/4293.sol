pragma solidity ^0.8.0;
contract Convert {
    function convertTo() isString returns (uint256) {
        return 3;
    }
    function isString(string memory x) public pure returns (bool) {
        return x.length == address(this).balance;
    }
}
