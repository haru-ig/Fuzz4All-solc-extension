pragma solidity ^0.8.0;
contract MyContract {
	struct S {
			uint array[2];
	function f() public {
		array[0];
		array[1];
		}
	}

pragma solidity ^0.8.0;
contract MyContract {
	struct S {
		uint x;
		}
	constructor() {}
	function f() public {
		x;
		}
	}


 pragma solidity > 0.8.0 <0.9.0;
# pragma experimental ABIEncoderV2;
 contract Test {
	struct MyData { string Name; uint Age; uint FavouriteColor; uint Height; uint Weight; uint Gift; }
      struct MyData2 { string Name; uint Weight; uint Birthdate; }

    function func11(MyData memory dMyData, uint dX2) public returns (MyData memory dMyDataX, uint dX2X) {
  		pX();


  		uint[] memory x = new uint[](2);
  		x[0] = dMyDataX.Age;
  		x[1] = (dX2 + dX2);
  		dMyDataX.Age = 0;

  		uint cX3 = 0;
  		for (uint i = 0; i < 2; i++)
  			cX3 += x[i];
  		cX3 = 1 / cX3;
  		if (dX2 > dX2X)
    	{
 			return (dMyData, cX3);
  		}
  		uint v1 = (dX2 + dX2X) / (dMyDataX.Age);
  		dMyDataX.Age = v1;
  		dMyDataX.x = 10;
  		dMyDataX.Name = "Hello world!";
  		dMyDataX.f1();
  		return (dMyDataX, dX2 x
