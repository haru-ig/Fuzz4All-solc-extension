pragma solidity ^0.8.0;
library Caller {
    function toggle(Caller caller) public view returns (bool) {
    byte data; data;
    return (data + data) % 2 == 1;
}
function get(Caller caller) public view returns (uint) { return caller.get(); }
}
