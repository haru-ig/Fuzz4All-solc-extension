pragma solidity ^0.8.0;
contract CallerFallbackMutated {
    function fallback() public payable {
        uint x = 0;
        while (address(this).balance < 90 { x += x; } ) {}
        x -= 1;
    }
}
