pragma solidity ^0.8.0;
contract SemanticSimilar354 {
  uint _state;
  assembly {

    switch mload(32) {

      case 101:
        _state = add(sub(add(mload(32), 0x130000000000000), _state), 100)

        if eq(sub(add(mload(32), 0x96000000000000), _state), 0){
          mstore(0x40, _state)

          fallback()
        }
      default:
        mstore(0x40, _state)
    }
  }
}
