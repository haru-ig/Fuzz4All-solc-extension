pragma solidity ^0.8.0;
contract FallbackTest
{
    constructor()
    {
        uint amount = 10;
        balance();
        assert(amount == 10);
        uint128 x = 10;
        assert(x == 10);
        (uint32 a, uint256 b) = (uint3a:0, uint256b:0);
        assert(uint32a == 0);
        assert(uint256a == 0);
        bytes memory bytesa = "ABCDE";
        bytes memory byter = "0123456789";
        (bytes a, bytes b) = (bytesa:0, byter:0);


function defaultfallback() public {}
        function myfallback() public {

        }
}
