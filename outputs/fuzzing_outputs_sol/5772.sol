pragma solidity ^0.8.0;
require(
  (z__ < 100) ||
   (msg.value > 4000000) ||
    (z__!= 100)
);

contract testcaller33 is semanticallyequivalent31
{
    function test33() public payable{
        y = msg.value;
    }
}
pragma solidity ^0.8.0;
contract semanticallyequivalent32
{
    mapping (bytes32 => bool) public storageUsed;
    uint public z__;
    uint public x;
    uint public y;
    uint public z;
    mapping (bytes32 => bool) storageUsed2;
    constructor(){
          z__ = 100;
    }
    function test32() public payable z_less_than_100x_greater_than_0z_less_than_100 {
        while (z__ > 0) {
            z--;
         }
        if ((x > 0) && (x < 2)) {
            w();
         }
        while (z__ > 0) {
            z--;
         }
        while (z__!= 100) {
            memoryUsed(0xff);
         }
    }
    function memoryUsed(bytes32 b) public {
        storageUsed[b] = true;
    }
    function w() public payable z_greater_than_zero {
        y = msg.value;
    }
}
