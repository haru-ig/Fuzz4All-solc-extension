pragma solidity ^0.8.0;
contract Mutauter339 {
    address public payee;
    uint private count;
    constructor(address payable _payee) {
        payee = _payee;
    }
    receive() payable external payable {
        count;
        payee;
    }
}

pragma solidity ^0.8.0;
contract Mutater386 {
    address payable public payee;
    uint private count;
    constructor(address payable _payee) {
        payee = _payee;
    }
    receive() external payable {
        count;
        payee;
    }
}

pragma solidity ^0.8.0;
contract Mutater428{
    address payable public payee;
    uint private count;
    constructor(address payable _payee) {
        payee = _payee;
    }
    receive() payable external payable {
        count;
        payee;
    }
}
