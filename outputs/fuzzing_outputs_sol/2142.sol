pragma solidity ^0.8.0;

pragma solidity ^0.8.0;

contract TypeErrors {
    mapping (uint8 array) test;
    function nonInteger(uint8 a) public {
      test[a]=new bytes(120);
    }
    function arrayLength(uint8 a, uint8[] memory b) public {
      test[a]=new bytes(120);
    }
    function array(uint8 a, uint8[] memory b) public {
      a==1;
      for (uint8 i0; i0 < b.length; i0++ )
      {
        if (b[i0] == 1)
        {
          1;
        }
      }
    }
}
