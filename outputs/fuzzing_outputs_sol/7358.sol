pragma solidity ^0.8.0;
contract ContractWithFallback {
    function () external payable {}
}
contract LowCostFallback{
  function fallback() external payable {
      ContractWithFallback caller;
      (,) call(address payable to, bytes memory data) external payable returns (uint256);
      (uint256 x, uint256 y) = address(caller).call{value:10}("");
      x;
      if false {
        int memory b = 0;
        uint i = 0;
        while (true){
          (,) call(address payable b, bytes memory c);
}
        (uint8 x) = (uint8) (256);
        uint i;
        if i > 1u/x {
            string memory name;
        }
(uint8 x) = (uint8) (13);
uint8 x;
      (uint256 x, x) = (uint256 {value:10});
      uint256 {value:10};

(uint256 x, ) = (uint256);
uint8 x;
        (uint256 x, uint16 ) = (uint256, (uint16));
        uint8 x;
        (uint256 x, uint8 ) = (uint256, (uint8));
        (uint256 x, x: uint256) = (uint256, (uint256));
        uint8 x: uint256;
        (uint256 x, uint256) = (uint256, (uint256));
      (uint256, uint256) = (uint256, (uint256));
      (uint256, ) = (uint256, (uint256));

      (uint8 x, bytes memory) = (uint8, bytes memory);
      (, bytes memory) = (bytes memory, bytes memory);
      bytes storage s;
      (bytes memory) = (bytes memory);
      uint i = 0;
      while (i < 1u) {
          uint c;
          bytes memory d;
          d;
          (, uint16) = (uint16, uint16);
          (uint16 x, ) = (uint16 {value:10}, uint16 {});
          uint16 x;
          (uint16 x, uint16 ) = (uint16 {value:10}, uint16 (uint16));
          uint8 x;
     }
      (uint256 x, uint256 ) = (uint256);
      uint8 x;
      (bytes memory, ) = (bytes,bytes);

(uint8 x) = (uint8) {value:10};
(uint8 x, bytes memory) = (uint8, bytes memory);
uint8 x;
(uint8 x, bytes memory) = (0x1000
