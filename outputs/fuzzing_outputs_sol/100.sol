pragma solidity ^0.8.0;
contract MyCallsCallAndFallbackFunctionThree {
    function getData(uint k) public {
        require(msg.value > 0, "Invalid call");
        uint256 n = 1;
        while (1 < msg.value) {
n += 1;
            n = x;
            if (n > x) {
                return;
            }
        }
        x = 1;
    }
    fallback() external payable { }
    receive() external payable {}
}
