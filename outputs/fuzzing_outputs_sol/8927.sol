pragma solidity ^0.8.0;
contract SemanticEquivalenceTo06 {
  uint public balance;
  emit(address payable owner);

  fallback() external payable {}

  receive() external {
    balance = address(this).balance;
  }
}
