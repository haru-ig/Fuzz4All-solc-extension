pragma solidity ^0.8.0;
contract MyCallsCallAndFallbackFunctionTwo {
    uint256 [3] data;
    function setData(uint y) public { data = [y, x, x + y]; }
    uint256 payable x;
    function getData(uint k) public view returns (uint55)
    { z = data[k]; }
    receive() payable { x = 1; }
    fallback() external payable {}
}
