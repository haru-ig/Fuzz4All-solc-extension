pragma solidity ^0.8.0;

contract Mutater334_2 {
    address payable public payee;
    address public payer;
    mutationCall() external payable {}
    constructor(address[] memory _payees, address _payer)
    {
        payee = _payees[0];
        payer = _payer;
    }
    mutationCall() function external payable
    {
        payee;
    }
    constructor() {
        payee = payer;
    }
}

pragma solidity ^0.8.0;

contract Mutater334_3 {

    address public payer;
    address public payee;
    function mutater() public payable {
        payer;
        payee;
        payee;
    }


    constructor(address _payer, address _payee, address payable _newPayee) {
        payee = _newPayee;
        payer = _payer;
    }
}
