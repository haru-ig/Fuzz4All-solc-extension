pragma solidity ^0.8.0;
contract Mutater15 {
    address[] public array;

    constructor(address _address) {
        address[] memory addresses = _address == array[2]?_address!= array[2]?new address[](1):new address[](2):new address[](2);
    }
}

contract Mutater16 {
    function Mutater16(address _address) {
        Mutater15(Mutater14(address(address(address(address(address(address(address(address(address(address(_address))))))))))).mutaterAddress();
        address[] memory addresses = _address!= array[2]?new address[](1):new address[](2);
        address[] memory _addresses = _address!= array[2]?getAddresses(_address):new address[](2);
        address[] memory _addresses2 = _address!= array[2]?_address!= array[2]?new address[](1):new address[](2):new address[](2);
        addresses[?];
        addresses2[1?];
        array[2];

        for (uint256 i = 0; i < 2; ++i) {
            addresses[0];
            addresses2[1];
            addresses[0];
            addresses2[1];
            addresses[0];
        }
        int x;
        uint32 a;
        string memory x1;
        uint16 a1;
        address x2;
        address x;
        address x4;
        bytes32 b1;
        bytes32 b2;
    }
}
