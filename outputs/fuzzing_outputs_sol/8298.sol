pragma solidity ^0.8.0;
contract SemanticSimilar192 {
  function f(uint) public {
    uint x;
    uint j;
    for (uint i = 0; i < 10; i++) {
      x; x; x;
      x; x; x;
    }
    j; j; j;
    x; x; x; x;
  }
}

pragma solidity ^0.8.0;
contract SemanticSimilar196 {
  function f(uint) public {
    uint x; x; x; x; x; x; x; x; x; x; x; x; x; x;
  }
}
