pragma solidity ^0.8.0;
contract SemanticSimilar206 {
  function f(uint, int) public {
    uint x; uint z;
    uint j;
    for (uint i = 0; i < 10; i++) {
      x; x; x; z; x; x; x; x; x; x;
    }
    z; x; x; x; x;
  }
}
